# 项目进度看板

> **最后更新**：2026-09-18 12:05 · **本轮完成**：P6-e 运价数据接入（**纠正 §2.3 错误**）+ 择时评估  
> 维护约定：**每轮工作结束必须更新本文件**，并在回复中给出「已实现 / 待办 / 待决策」。



---

## 一、一句话状态

**⚠️ 本轮纠正了两个错误**：① 设计文档 §2.3 说「akshare 没有 BDTI」是**错的** —— BDTI 有 **25 年历史**，已落库 40340 行；  
② 运价数据**不能用截面 IC 评估**（全行业共同指标，截面零变异）→ 新增**分层择时**方法。  
**用正确方法评估后，8 个运价择时信号 0 个通过。**  
**因子库最终：78 因子 + 8 择时信号，全部不通过门槛。** 建议转向把工程链路做完（P6-a → P7 → P8）。

**整体进度：约 92%**

---

## 二、阶段进度总览

| 阶段       | 内容                                       | 状态      | 单测  |
| -------- | ---------------------------------------- | ------- | --- |
| **P0–P3.6** | 骨架 / 规则层 / 数据层 / 引擎层 / 费率           | ✅ 完成    | 388 |
| **P4**   | `evaluation/` 评估 + 鲁棒性 + 对账              | ✅ 完成    | 88  |
| **P5**   | `strategy/` 因子库 + 系统化搜索                  | ✅ 完成    | 224 |
| **P5b**  | 估值因子（PIT 验证）                             | ✅ 完成    | 66  |
| **P6b**  | 多因子合成 + split-half                      | ✅ 完成    | 21  |
| **P6c**  | 业绩预告因子（正交源验证）                            | ✅ 完成    | 27  |
| **P6d**  | 正交源探针 + t 值自相关修正                          | ✅ 完成    | 9   |
| **P6d2** | 财务质量因子（8 个，报告期采样）                          | ✅ 完成    | 36  |
| **P6e**  | **运价数据 + 择时评估（纠正 2 个错误）**                  | ✅ **完成** | 33  |
| P6-a     | `execution/` RiskGate + OMS              | ⬜ **下一轮** | —   |
| P7       | Walk-Forward + Monte Carlo + 日历季节性择时    | ⬜ 未开始   | —   |
| P8       | `app/` + 实盘通道                            | ⬜ 未开始   | —   |

---

## 二、阶段进度总览

| 阶段       | 内容                          | 状态         | 单测  |
| -------- | --------------------------- | ---------- | --- |
| **P0**   | 骨架 / 配置 / 架构守卫              | ✅ 完成       | 45  |
| **P1**   | `rules/` A股规则层              | ✅ 完成       | 133 |
| **P2**   | `data/` 数据层 + `PITGuard`    | ✅ 完成       | 114 |
| **P3**   | `core/` 引擎层                 | ✅ 完成       | 101 |
| **P3.5** | 公司行为接入引擎                    | ✅ 完成       | 15  |
| **P3.6** | 费率定稿 + 滑点模型 B               | ✅ 完成       | +4  |
| **P4**   | `evaluation/` 评估 + 鲁棒性 + 对账 | ✅ 完成       | 88  |
| **P5**   | `strategy/` 因子库 + 截面 IC     | ✅ 完成       | 92  |
| P6       | `execution/` RiskGate + OMS | ⬜ 未开始      | —   |
| P7       | Walk-Forward + Monte Carlo  | ⬜ 未开始      | —   |
| P8       | `app/` + 实盘通道               | ⬜ 未开始      | —   |

**整体进度：约 76%**（P0–P5 完成；P6–P8 为风控/鲁棒性/实盘）

---

## 三、已实现

### 3.1 `do_modle/objects.py`（250 行）— 全系统数据契约

`Instrument` / `Bar`（含 `prev_close` `is_suspended` `is_st` `limit_up/limit_down` `adjust_flag`）/  
`Trade`（费用逐项拆分）/ `OrderRequest` / `Signal` / `AccountSnapshot`（当日 + 累积双口径）/  
`CorporateAction` / `Verdict` / `RejectReason` / `Side` / `Board` / `AdjustFlag`

### 3.2 `do_modle/numeric.py`（52 行）— 数值工具

`round_to_tick`（Decimal HALF_UP，避免浮点误差）/ `floor_to_lot` / `ceil_to_lot` / `round_half_up`

### 3.3 `do_modle/config.py`（165 行）— 配置加载

YAML → 冻结 dataclass；`deep_merge`；未知键忽略；`config/{base,stock_601872}.yaml`

### 3.4 `do_modle/rules/`（988 行）— A股规则层 ★ 零外部依赖

| 模块                    | 类                                    | 关键能力                                                                                                                    |
| --------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| `cost.py`             | `AShareCostModel`                    | **万一（0.0001）+ 免五**；佣金/印花税/过户费/滑点逐项拆分；**滑点至少 1 tick**；往返成本率 **0.072%**（含滑点 0.172%）                                       |
| `position.py`         | `PositionBook`                       | **FIFO 批次队列实现 T+1**；`sellable = total − today_bought − frozen`；送转取整误差用 `expected_quantity` 校正；**双盈亏口径**（累积 vs 当日，昨仓用昨收） |
| `tradability.py`      | `LimitRuleTable` + `TradabilityGate` | 板块/ST/**历史时期**/新股窗口 涨跌幅表；**涨跌停非对称**（涨停可卖、跌停可买）；停牌/一字板/流动性/手数                                                            |
| `corporate_action.py` | `CorporateActionHandler`             | 送转 + 现金分红；**配股显式拒绝**                                                                                                    |
| `portfolio.py`        | `Portfolio`                          | 多标的容器（阶段一只有 1 个 key，接口已按多标的定义）                                                                                          |
| `account.py`          | `Account`                            | 资金/冻结/日终快照；`deposit`（分红流入）；`max_buy_quantity`                                                                           |

### 3.5 `do_modle/data/`（1212 行）— 数据层

| 模块                         | 能力                                                                                      |
| -------------------------- | --------------------------------------------------------------------------------------- |
| `provider.py`              | `DataProvider` 抽象 + `PROVIDERS` 注册表；3 个必须实现 + 4 个可选默认                                   |
| `pit.py`                   | **`PITGuard`**：11 类数据的可见时点规则表；`assert_no_lookahead` **抛异常**而非静默过滤；未知 kind 报错；年报滞后 120 天 |
| `cache.py`                 | `DataCache`（SQLite）：**幂等 upsert**、历史行不被修改、表名/列名白名单防注入                                   |
| `adapters/baostock_src.py` | baostock 适配器；**纯解析函数与网络解耦**；`resolve_baostock_func` 解析未导出的函数；分红字段候选名 + 实机探测             |
| `adapters/local_cache.py`  | `CachedProvider`：回测从本地 SQLite 读，不再联网；**前复权直接拒绝**（会引入前视）                                 |

### 3.6 `do_modle/core/`（1045 行）— 引擎层

| 模块            | 能力                                                                                 |
| ------------- | ---------------------------------------------------------------------------------- |
| `event.py`    | `EventBus` + 7 种事件类型；重复订阅只注册一次；handler 可在发布中退订                                     |
| `clock.py`    | `Clock`：回测/实盘统一时间源；`advance/at/next_date/previous_date`                            |
| `matching.py` | `MatchingEngine`：**唯一撮合点**；成交价写死 `next_open`；**未来函数守卫**；**先卖后买**；资金缩减；撮合无副作用       |
| `ledger.py`   | `Ledger`：**唯一账务点**；成交 → 现金 + 持仓；已实现盈亏；**公司行为**（分红入账 + 送转调整）                        |
| `recorder.py` | `Recorder`：逐日快照，固定字段顺序                                                             |
| `backtest.py` | `BacktestEngine`：T 日出信号 → **T+1 开盘成交**；日单有效期 1 交易日；除权除息在盘前应用；风控钩子 `pre_trade_hook` |

### 3.7 脚本（794 行）

| 脚本                          | 用途                              |
| --------------------------- | ------------------------------- |
| `scripts/verify.py`         | **一键验证**：环境自检 + 单测 + 连通性 + 字段核对 |
| `scripts/sync_data.py`      | 数据同步落库（2591 交易日）                |
| `scripts/validate_bh.py`    | **B\&H 成本校验**（支持含分红区间）          |
| `scripts/probe_baostock.py` | 早期探针（已被 verify.py 覆盖）           |
| `py.cmd`                    | PowerShell 友好启动器                |

### 3.8 数据资产

`data_store/market.sqlite`（740 KB）：`daily_bar_none` 2591 行 · `daily_bar_hfq` 2591 行 ·  
`calendar` 2591 行 · `instrument` 1 行 · `dividend` 13 行

---

## 四、待办

### 下一轮（P4）— 评估与鲁棒性层

| #    | 任务                        | 说明                                                            |
| ---- | ------------------------- | ------------------------------------------------------------- |
| P4-0 | **akquant 对账探针**          | 5 个边界场景，记录两引擎**实际行为**。已核验 akquant 不读 `Bar.extra`，故对账须"喂已裁剪订单" |
| P4-1 | `evaluation/perf.py`      | quantstats 封装；**基准为必填参数**                                     |
| P4-2 | `evaluation/pitfalls.py`  | 10 项陷阱清单**可执行断言**                                             |
| P4-3 | `evaluation/reconcile.py` | 对账范围限定 8 项（成本/滑点/价格基准/tick/手数/成交量/资金持仓/T+1）                   |
| P4-4 | 全区间 B\&H 校验               | 2016–2026 全区间（含 13 条分红）残差验证                                   |

### 后续

| 阶段 | 主要任务                                                 |
| -- | ---------------------------------------------------- |
| P5 | 因子库（动量/趋势/波动/量能/估值/季节性/事件）+ **航运板块截面 IC（N≥10）**      |
| P6 | `RiskGate` 9 条规则 + `OrderManager` 状态机                |
| P7 | Walk-Forward（WFE）+ Monte Carlo（打乱/噪声/**执行延迟**/参数敏感性） |
| P8 | CLI + 每日任务（含 `DataReadinessCheck`）+ 纸面交易             |

---

## 五、待你决策

| # | 决策       | 选项                              | 我的建议                    | 影响面        |
| - | -------- | ------------------------------- | ----------------------- | ---------- |
| 1 | 样本内/外切分点 | 暂定 `in_sample_end = 2021-12-31` | 接受，并加 Walk-Forward 多滚动段 | P7         |
| 2 | 初始资金     | 暂定 20 万                         | 按实际资金定                  | P4 B\&H 校验 |

> ✅ **原「滑点模型」决策已定稿**（用户选 B：至少 1 tick），已实现并验证。详见 §八 变更日志。

### 已定稿的费率与滑点（2026-09-17）

| 项         | 值                                 |
| --------- | --------------------------------- |
| 佣金率       | **万分之一（0.0001）**                  |
| 最低佣金      | **0（免五）**                         |
| 印花税       | 0.0005（仅卖出）                       |
| 过户费       | 0.00001（双边）                       |
| 滑点        | 0.05%，**且至少 1 tick**              |
| **往返成本率** | **0.072%**（不含滑点）/ **0.172%**（含滑点） |

**滑点模型 B 的副作用（已知并接受）**：1 tick 占股价比例随价格下降而上升。

| 股价          | 1 tick 占比 | 百分比模型(0.05%) | 模型 B 取值 | 倍率    |
| ----------- | --------- | ------------ | ------- | ----- |
| 3.30 元      | 0.303%    | 0.00165 元    | 0.01 元  | ≈6×   |
| 4.88 元      | 0.205%    | 0.00244 元    | 0.01 元  | ≈4×   |
| 6.08 元      | 0.164%    | 0.00304 元    | 0.01 元  | ≈3.3× |
| 10.00 元     | 0.100%    | 0.00500 元    | 0.01 元  | ≈2×   |
| **20.00 元** | 0.050%    | 0.01000 元    | 0.01 元  | 1×    |
| 40.00 元     | 0.025%    | 0.02000 元    | 0.02 元  | 百分比胜出 |

**交叉点 = 20 元**。低于 20 元时 1 tick 主导，高于时百分比主导。

**这个"放大"更真实而非纯粹保守**：4.88 元的股票上，"0.05% 滑点" = 0.24 个 tick，**物理上不可能实现**。  
真实成交要么原价、要么 ±1 tick。所以百分比模型在低价股上本身就错，tick 量化才是正确形态。  
若后续发现低价区间被过度惩罚，可用 `CostConfig(slippage=0.0)` 做敏感性对照。

---

## 六、验收门槛核对

| #  | 门槛                                    | 状态                         |
| -- | ------------------------------------- | -------------------------- |
| 1  | 4 条 import 禁令自动断言通过                   | ✅ 7 用例，且已验证会真实失败           |
| 2  | T+1 超卖被拒                              | ✅                          |
| 3  | 涨停不可买 / 跌停不可卖                         | ✅ 含非对称用例                   |
| 4  | 停牌、一字板被拒                              | ✅                          |
| 5  | 往返成本率 0.072% / 含滑点 0.172%             | ✅                          |
| 6  | 除权后数量与成本正确                            | ✅                          |
| 7  | `as_of` 调早一天时未来数据不可见                  | ✅                          |
| 8  | 增量 upsert 幂等，历史行不被改                   | ✅                          |
| 9  | 同输入两次运行净值逐点相等                         | ✅                          |
| 10 | 成交价 = `bar_{T+1}.open × (1±slippage)` | ✅                          |
| 11 | **B\&H 与手工持有差异完全由成本解释**               | ✅ **残差 0.0000**（含分红区间同样通过） |
| 12 | akquant 对账差异 < 0.5% 或可解释              | ⬜ P4-0                     |
| 13 | 10 项陷阱清单全部通过                          | ⬜ P4                       |
| 14 | 每个因子通过无前视校验                           | ⬜ P5                       |
| 15 | 风控规则全部可触发并有记录                         | ⬜ P6                       |
| 16 | 参数热力图存在宽稳定区                           | ⬜ P7                       |
| 17 | 执行延迟 1/2/3 日衰减曲线已产出                   | ⬜ P7                       |
| 18 | 纸面交易信号一致率 / 模拟成交一致率 100%              | ⬜ P8                       |
| 19 | 换 `symbol` 不改代码可跑通                    | ⬜ P8                       |

**11/19 已达成**（其中第 11 项是 P4 的核心门槛，已提前完成）

---

## 七、关键数字

| 指标             | 值                                            |
| -------------- | -------------------------------------------- |
| 单测（隔离环境）       | **941 passed**                               |
| 单测（无 baostock） | **936 passed, 5 skipped**                    |
| 生产代码           | **7335 行**（36 个文件）                           |
| 测试代码           | **6527 行**（26 个测试文件）                         |
| 因子库            | **78 个 / 14 类别**（47 价格量能 + 11 估值 + 5 预告 + 8 财务质量 + 7 运价） |
| **运价数据**        | **6 指数 · 40340 行**（BDTI 2001 年起，25 年）        |
| **择时信号**        | **8 个，0 个通过**（分层回测 + 不重叠采样）               |
| 财务数据           | **6 表 × 397 行**（`pubDate` 100% 非空，PIT 安全）      |
| 研究池数据          | **12 只标的 · 22,703 根 K 线**（2016-01-04 ~ 2026-09-01） |
| 估值数据填充率        | `pe_ttm`/`pb_mrq`/`ps_ttm`/`pcf_ncf_ttm` **100%**，`turnover` 98.9% |
| 预告数据           | **195 行**（含公告日，PIT 安全）                      |
| 截面平均 N         | **8.7**（早期 6 → 后期 12）                        |
| **往返成本率**      | **0.072%**（不含滑点）/ **0.172%**（含滑点）            |
| **全区间 B&H**    | 200,000 → **696,727**（+248.36%，年化 +12.48%，最大回撤 −39.47%） |
| **独立校验残差**     | **−0.0000**                                  |
| **akquant 对账** | **无不一致项**                                   |
| **71 因子搜索**    | **0 存活**（修正 t 后 ① 从 25/63 → **0/63**）        |
| **报告期采样最好**    | `fin_yoy_ni` \|IC_IR\| = **0.335**（T=35, t=−1.98 不显著） |
| **t 值膨胀倍数**    | **2.00~2.05**（自相关修正，与理论 √(1+2×0.8)=2.0 吻合） |
| **多因子合成 OOS**  | **+0.1546 / +0.1123**（门槛 0.30）；增益被相关性锁死      |
| **预告因子正交性**     | 平均 \|相关\| **0.044**（其他因子间 0.455，**差 10 倍**）      |

---

## 八、变更日志

### 2026-09-18 · P6-e 运价数据接入 + 择时评估（**纠正 2 个错误**）
- **⚠️ 纠正错误 1**：设计文档 §2.3 说「**akshare 没有 BDTI**」是**错的**
  - 核查源码：`macro_china_bdti_index()` **存在**（东财 `EMI00107668`）
  - 实测：**BDTI 6018 行，2001-12-27 起（25 年）**；BDI 9541 行（38 年）
  - **已落库 40340 行**（6 个指数）
  - **更优做法**：直接调东财公开 API，**无需引入 akshare**
  - 设计文档 §2.2 / §2.3 **已修订**
- **⚠️ 纠正错误 2**：运价数据**不能用截面 IC 评估**
  - 实测：同日 6 只标的的 BDTI 取值个数 = **1**（全行业共同指标，截面零变异）
  - 截面 IC 恒为 nan；**这暴露了框架问题** —— 阶段一是单标的择时，不该用选股方法评估
  - **新增 `evaluation/timing.py`**：`quantile_timing()` 分层择时评估
    - 按信号分位分组 → 比较各组未来收益 → 检验**单调性** + 顶底差显著性
    - **不重叠采样**（每 horizon 日取 1 个观测）消除重叠窗口自相关
- **交付**：
  - `scripts/sync_shipping.py` —— 东财 API 抓 6 个航运指数
  - `Bar` 新增 `ship_bdt` / `ship_bdi` / `ship_bct`
  - **`panel.attach_shipping()`** —— 数据日期 + 2 天可见，**不插值**（carry-forward）
  - 7 个运价因子（71 → **78**）
  - `scripts/timing_test.py` —— 运价择时评估
- **择时评估结果：8 个信号 0 个通过**
  - 全部不显著（|t| 最大 1.31 vs 阈值 2.733）且**全部不单调**
  - 最有经济含义的「运价-股价背离」方向正确（顶底差 +3.57%）但 t=+1.31
- **关于霍尔木兹**：实测公开追踪器**只有 ≤1 年历史**，无法回测
  → 不作 alpha 因子，可作**实盘风控输入**
- 单测 908 → **941**

### 2026-09-18 · P6-d-2 财务质量因子（**因子层系统性穷尽**）
- **数据管线**：`sync_financial.py` 抓取 **6 表 × 397 行**，3168 任务 / 26.7 分钟 / **零失败**
  - **踩坑**：初版把**表名**传给 `getattr(bs, ...)` → 全部 `None` → 3168 任务 2 秒空转且**报"无失败"**
    → 修接口名映射 + **新增"0 行即 FAIL"校验**
  - 接口名不规则（`query_cash_flow_data` 带下划线）→ 显式映射表，不靠拼接
- **8 个财务质量因子**：`fin_roe` / `fin_gp_margin` / `fin_np_margin` / `fin_asset_turn` /
  `fin_liab_to_asset` / `fin_yoy_ni` / `fin_cfo_to_or` / `fin_dupont_roe`（因子库 63 → **71**）
- **PIT**：`attach_financials` —— **优先用实际公告日 `pubDate + 1 天`**，
  无 `pubDate` 时退回「报告期 + 法定滞后」；`max_age_bars=250`
- **⭐ 核心方法论**：`xsec.report_observations` + `xsec.build_report_sections`
  - **按报告期采样**（每标的每季一个观测），彻底避免日频采样的自相关
  - **每个因子用其自己的信息更新事件日**作为观测点
- **结果（日频 vs 报告期双口径）**：
  | 因子 | 日频 t | 报告 T | 报告 \|IC_IR\| | 报告 t |
  |---|---|---|---|---|
  | fin_yoy_ni | −1.27 | 35 | **0.3353** | −1.98 |
  | fin_np_margin | −1.11 | 35 | 0.3024 | −1.79 |
  | turnover_mean_20 | +3.30 | 35 | **0.3801** | +2.25 |
  - **报告期采样 IC_IR 显著高于日频**（财务 0.09 → 0.34）→ **日频采样稀释信号**
  - **财务因子 IC 全为负** → 航运板块**低质量溢价**（周期高点财务漂亮但该卖）
  - `fc_score` 报告期 T=**3** → **标注为不可评估**（预告集中在 1/7 月，无法按季分组）
- **结论**：71 个因子无一带自相关修正后通过门槛；**因子层已系统性穷尽**
- 单测 872 → **908**

### 2026-09-18 · P6-d 正交源探针 + ⚠️ **t 值自相关修正（推翻此前显著性结论）**
- **探针结果**：
  - **baostock 财务接口字段完美**：`query_profit_data` 含 **`pubDate`（公告日）** +
    `statDate`（报告期），例 `pubDate=2020-08-28` / `statDate=2020-06-30` → **PIT 可行**
  - 但 baostock 曾因高频调用被**会话级限流**（连续约 700 次后**连简单日线查询也挂死**）
  - **futu-mcp 整体不可用**（连 `quote_stock_quote` 都 internal error）
  - 已落库数据**完好**，不受影响
- **⚠️ 发现并修正一个严重方法论缺陷**：
  - **IC 序列高度自相关**（实测 lag-1 **0.64~0.69**），根源是**前向收益窗口重叠**
    （5 日前向收益 → 相邻日 IC 有 4/5 区间重叠 → 理论上限 ≈ 0.8）
  - **朴素 `t = IC_IR × √T` 系统性高估 2 倍**（实测膨胀 2.00~2.05，与理论 √(1+2×0.8)=2.0 吻合）
  - 修正：`T_eff = T/(1+2Σρ_k)`（Bartlett 截断），`t_correct = IC_IR × √T_eff`
  - **修正后「① t 过 Bonferroni」从 25/63 → 0/63**
  - `turnover_mean_20` 修正 t = **3.30**（朴素 6.65），**极接近阈值 3.354**
- **`ICSeries` 新增**：`t_stat_naive` / `effective_sample_size` / `t_stat`（正式判据）/ `t_inflation`
  - `t_stat` 语义已改，所有下游脚本自动生效；**9 个专项测试**
- **`scripts/sync_financial.py`**（新增）：**限流友好**的财务数据抓取
  - `sleep` + **断点续传** + 失败隔离 + 失败清单落盘
  - **踩坑**：初版把**表名**传给 `getattr(bs, ...)` → 全部 `None` → 3168 任务 2 秒空转且**报"无失败"**
    → 修接口名映射 + **新增"0 行即 FAIL"校验**
- 单测 863 → **872**

### 2026-09-18 · P6c 业绩预告因子（**找到真正正交的信息源**）
- **先修正决策冲突**：用户指示「A+C 并行」，C 原指运价。但设计文档 **§2.3 已决策排除运价数据**
  （BDTI 无源 + PIT 成本高 + 已六次验证无效）。§2.3 批准但未实现的是 **`业绩预告` + `日历季节性`**
  → **修正 C = 业绩预告**
- **日历季节性的设计澄清**：**不能做截面因子**（同日所有标的同值 → 截面方差 0），
  属时序择时层，留待 P7
- **数据探针**：`query_forecast_report` 返回 **195 行**（含公告日 `profitForcastExpPubDate`）
- **PIT 零新增**：复用已有 `KIND_EXPRESS_REPORT`（公告日 +1 天 09:00）
- **交付**：
  - `Forecast` 数据类 + `FORECAST_KIND_SCORE` 类型打分表
  - `Bar` 新增 3 字段（`fc_score`/`fc_chg_pct`/`fc_age_days`）
  - `get_forecasts()` + `parse_forecast_row()` / `parse_express_row()`
  - **`strategy/panel.attach_forecasts()`** —— PIT 核心（可见日 + **信息衰减 `max_age_bars=120`**）
  - 5 个预告因子（因子库 58 → **63**）
- **搜索结果（k=63, z=3.354）**：
  - ① t 过 Bonferroni：25/63
  - ② Walk-Forward 稳定：**0/63**；③ 全期 \|IC_IR\| ≥ 0.3：**0/63**
  - **`fc_score`：IC_IR=−0.0990（利好出尽）、t=−4.08、符号一致 90%、maximin 最差窗 0.0458（全库第 1）**
- **⭐ 结构性突破**：`fc_score` 与其他因子平均 \|相关\| = **0.044**（其他因子间 **0.455**，差 10 倍）
  - **"换信息源"的思路被实证验证正确**
  - 理论增益 1.33× → 1.52×；但单源强度不足（0.099），实测 OOS 仅 +0.1072 → +0.1123
- **关键推论**：若再找 3–5 个同类正交源 → 增益 2.6× → 0.1325×2.6 = **0.34 > 0.3**，**跨门槛可能**
- 单测 836 → **863**

### 2026-09-18 · P6b 多因子合成（split-half 样本外）
- **`strategy/composite.py`**（新增）：横截面 z-score / 排名 + 等权合成 + 因子相关性（21 用例）
  - **只做等权，不做 IC 加权**（避免参数拟合）
  - 缺失标的按可用因子**权重重归一化**（不丢整个标的）
- **`numeric.py`**：**下沉** `rank`/`spearman`/`pearson`/`mean`/`std`
  - **架构修正**：`composite.py` 原直接 import `evaluation.xsec`，**违反红线**
    （`strategy` 不得 import `evaluation`）→ 秩统计下沉到 `numeric.py`，两层共用
- **`scripts/factor_combo.py`**（新增）：split-half 样本外评估 + 相关矩阵 + 理论增益
- **结果**：
  - 合成 OOS IC_IR = **+0.1546**（前半→后半）/ **+0.1072**（后半→前半）
  - 单因子最好 0.1325 → 合成 0.155（+17%），**仍低于门槛 0.30**
  - **平均 \|因子相关\| = 0.455**（`turnover_mean_20`↔`turnover_mean_60` = 0.92）
  - 理论增益上限 √(5/(1+4×0.455)) = **1.33×**；0.1325×1.33 = 0.176，**与实测吻合**
- **定论**：继续在现有 58 因子上做组合优化，**数学上不可能跨过门槛**
  - 要达标需：单因子 ≥0.226（现 0.133）或 ρ̄=0（航运板块不可能）或 N→100（偏离目标）
- 单测 814 → **836**

### 2026-09-18 · P5b 估值因子（PIT 已验证）+ 58 因子搜索
- **PIT 安全性实证验证（前置门槛）**：`close/pbMRQ`（=BVPS）的 8 个跳变点中 **7 个不在季末**，
  2016-06-30/09-30/12-31 **全部不跳变** → baostock 按财报公布日更新，**PIT 安全**
- **数据管线**：
  - `Bar` 新增 5 个可选字段（`pe_ttm`/`pb_mrq`/`ps_ttm`/`pcf_ncf_ttm`/`turnover`）
  - **关键发现**：这 5 个字段**原本已在 `DAILY_FIELDS` 查询里**，只是被 `row_to_bar` 丢弃
  - `cache.py` 新增 **schema 迁移**（`ALTER TABLE ADD COLUMN`）→ 加列无需删表重建
  - `local_cache.py` 新增 `_opt()` 可空数值解析
- **11 个估值因子**：`ep_ttm`/`bp_mrq`/`sp_ttm`/`cfp_ttm`/`turnover_mean_20/60`/
  `turnover_ratio_20`/`ep_mom_60`/`bp_mom_60`/`ep_vol_60`/`ep_pct_250`
- **搜索结果（k=58, z=3.332）**：
  - ① t 过 Bonferroni：18/47 → **24/58**
  - ② Walk-Forward 稳定：**0/58**
  - ③ 全期 \|IC_IR\| ≥ 0.3：**0/58**
  - IC_IR 天花板 0.1158 → **0.1325**（`turnover_mean_20`，t=+6.65）
  - `turnover_mean_60` 是唯一进入 maximin 前 6 的新增因子
- **方法论反思（重要）**：推导出 `IC_IR = ρ × √(N−1)`，故 N=8.7 时 `IC_IR≥0.3`
  **恰好等价于经典「IC≥0.1」门槛** → **门槛标定正确，不是过严**
  - 实测 `std_IC≈0.44` vs 理论 `1/√7.7=0.36`，验证推导
  - 最好因子真实 ρ≈0.056 < 门槛 0.108
- 单测 748 → **814**

### 2026-09-18 · P5 因子库扩充（47 个）+ 系统化搜索
- **`strategy/factors.py`**：8 → **47 个因子 / 10 类别**（参数化生成）
  - 新增：`price_pos_*`、`high_prox_250`、`max_ret_*`（彩票）、`skew_60`、
    `downside_ratio_60`、`amihud_20`（流动性）、`vp_corr_20`、`turnover_trend_20`、
    `intraday_range_20`、`consec_up`、`gap_freq_20`、`accel_20_60`、多周期 `mom/rev/vol/ma_gap/rsi/vratio`
  - `min_bars` 语义改为**恰好需要的 K 线数** → `panel.py` 精确切片，O(n²) 降为 O(1)
- **`evaluation/xsec.py`**：新增 `rolling_windows` / `evaluate_stability` / `StabilityReport`（Walk-Forward）
- **`scripts/factor_search.py`**（新增）：**三重闸系统化搜索**
  - ① 全空间 Bonferroni（k=47 → z=3.273）
  - ② Walk-Forward 稳定（每窗 \|IC_IR\|≥0.10 且符号一致≥80%）
  - ③ 全期 \|IC_IR\| ≥ 0.3
  - **排序用 maximin（按最差窗口），而非"挑全期最大"**
- **搜索结果**：
  - ① **18/47** ② **0/47** ③ **0/47** → **无因子存活**
  - 最稳的 `ma_gap_120` 最差窗仅 0.0378；全期最高的 `intraday_range_20`（0.1158）最差窗塌到 0.0101
  - 报告明确标注：「若挑 IC_IR 最大，k=47 时纯噪声下至少一个显著的伪概率 = 90%」
- 单测 616 → **748**

### 2026-09-18 · P5 分层检验（样本内外 + 市场状态）
- `evaluation/xsec.py` 新增 `filter_sections` / `split_sections` / `momentum_state` / `compare_segments`（8 用例）
- `scripts/xsec_ic.py` 重写：支持 `--split`（样本内外）与 `--stratify`（市场状态分层）
  - 新增**符号一致性**与**强度一致性**（强度比 > 5 → 对市况敏感）两重交叉验证
- **关键结论（精细化）**：
  - 样本内外：**7/8 符号一致**（随机概率 0.78%）；`mom_120` 翻转
  - 市场状态：**8/8 符号一致**（随机概率 0.39%）
  - **5 个因子方向稳健**：`reversal_5` / `mom_20` / `mom_60` / `ma_gap_20` / `volume_ratio_20`（强度比 1.1~2.2）
  - **3 个对市况敏感**：`vol_20`（27.6 倍）、`mom_120`（13.1 倍）、`rsi_14`（5.0 倍）
  - **但强度全部不足**：|IC_IR| ≤ 0.13，门槛 0.3
- **判据升级为三档**：方向一致+强度不足 → 「真实但微弱的信号」；方向不一致 → 噪声；强度比 >5 → 敏感不可用
- 单测 608 → **616**

### 2026-09-18 · P5 因子库 + 首轮截面 IC（否定性结论）
- **`evaluation/xsec.py`**：截面 IC 框架（32 用例）。`CrossSection` / `ICSeries` / `spearman` / `build_cross_sections`
  - **主指标 IC_IR**，必须同时报 **N**（平均截面标的数）
  - `NOISE_FLOOR` 表：N=1→1.0，N=10→0.316，N=12→0.289
- **`strategy/factors.py`**：8 个因子（纯函数，PIT 安全）+ 注册表
- **`strategy/panel.py`**：因子/收益面板构建（PIT 守卫 + 停牌跳过）
- **`config/universe_shipping.yaml`**：航运板块 12 只研究池
- **`scripts/xsec_ic.py`**：截面 IC 报告（**三重门槛判读**）
- **数据扩充**：12 只标的 **22,703 根 K 线**入库
  - `sync_data.py` 加**断线重连 + 3 次重试**（baostock 长连接批量查询会断）
- **结论：8 个因子全部未通过三重门槛**
  - ① `|mean_IC|` > 噪声下界（0.339）：**0/8**（最大仅 0.0366，差 9 倍）
  - ② `|t|` ≥ Bonferroni 2.733：5/8
  - ③ `|IC_IR|` ≥ 0.3：**0/8**（最大 0.082）
  - **动量因子 IC 全为负** → 航运板块具反转特征
- 单测 524 → **608**

### 2026-09-17 · P4 完成（对账 + 文档）
- **`evaluation/reconcile.py`**：按探针实测范围实现对账。24 用例
  - **四态结论**：`MATCH` / `MISMATCH` / `KNOWN_DIFF` / `SKIP`
  - 硬要求：喂**已裁剪订单**、`volume_limit_pct=0`、`t_plus_one=True`
  - 费用比较用「自建 commission + transfer_fee」对「akquant commission」
- **T+1 探针修复**：改用观测 `available_position`，得到**决定性证据**
  - `t_plus_one=True` → 持仓 100 / 可用 **0**（T+1 生效）
  - `t_plus_one=False` → 持仓 100 / 可用 **100**（不生效）
- **发现一个真实模型差异（非 bug）**：低价股上自建 `6.09`（1 tick 对齐）
  vs akquant `6.08304`（**纯百分比，不取整到 tick**）
  → 新增 `KNOWN_DIFF` 状态，不计入失败
- **`scripts/reconcile_akquant.py`**：实机对账 → **无不一致项**
- 单测 500 → **524**

### 2026-09-17 · P4 部分（评估层 + P4-0 探针 + 全区间校验）
- **`evaluation/perf.py`**：原生绩效指标（不依赖 quantstats）+ quantstats HTML（可选）。29 用例
  - 相对基准指标：超额收益 / 信息比率 / 跟踪误差 / beta / alpha / **超额 t 值**
  - **基准为必填语义**：缺失时 `relative()` 抛错（不允许只报绝对收益）
  - Sortino 在无负收益时返回 `inf` 而非误导性的 `0.0`
- **`evaluation/pitfalls.py`**：10 项陷阱清单**可执行断言**（PASS/FAIL/WARN/SKIP）。37 用例
  - 含 **Bonferroni 校正**表（`bonferroni_z(n)`）
  - 第 7、10 项诚实标为 WARN/SKIP（需人工 / 归属 P7），**不假装通过**
- **`scripts/probe_akquant.py`**：**P4-0 对账探针**（5 场景）→ `probe_report.md`
- **`scripts/validate_full.py`**：全区间 B&H **独立校验**（分红与送转由脚本自行重算）
- **🐛 修复一个真实 bug**：`CorporateActionHandler.apply` 的**顺序反了** —— 原来先送转再派息，
  导致分红基数被放大。独立重算发现 13 次除权累计**多派 24.80 元**。已改为**先按登记日持仓派息、再送转**
- 单测 431 → **500**

### 2026-09-17 · P3.6 费率定稿 + 滑点模型 B

- **佣金改为万一（0.0001）+ 免五**（原万五）→ 往返成本率 **0.152% → 0.072%**
- **滑点模型 B**：`slippage > 0` 时成交价**至少移动 1 tick**，避免低价股滑点被取整抹成 0
- 实测生效：2016-06-02 成交价 `4.88 → 4.89`；2024-01-03 `6.08 → 6.09`；滑点 `0 → 10.0000`
- 含分红 / 无分红两个区间的 B\&H 残差**仍为 0.0000**
- 同步更新 `config/base.yaml`、`CostSettings`、全部相关测试期望值
- 单测 427 → **431**（新增 4 个滑点模型用例）
- **记录副作用**：模型 B 在低价股上放大滑点（3.30 元时 ≈6 倍），但这是**更真实**的 tick 量化，非纯保守

### 2026-09-17 · P3.5 公司行为接入引擎

- `Ledger.on_corporate_action`：分红入账 + 送转调整；`Account.deposit`
- `BacktestEngine` 在**除权日盘前**应用公司行为（股权登记日 T-1，除权日 T）
- 新增 `EventType.CORPORATE_ACTION`；`BacktestResult.corporate_actions` / `dividend_cash`
- **修正一条重要语义**：**T 日买入不享有 T 日分红**（引擎"先应用公司行为、再执行订单"的顺序是刻意的），已加测试固化
- `validate_bh.py` 支持含分红区间；实测 2016-06-01~08-01（分红 66 元）**残差 0.0000**
- 单测 411 → **427**

### 2026-09-17 · P3 引擎层 + 实机验证

- `core/` 六个模块；411 单测
- 实机同步 2591 交易日；B\&H 成本校验残差 0.0000
- 发现并修复 `query_stocks_in_risk` 服务端不支持
- **发现滑点被 tick 取整吃掉**（待决策）
- 新增 `py.cmd`；`verify.py` 加解释器自检；测试对可选依赖健壮化

### 2026-09-17 · P2 数据层

- `data/` 四个模块 + baostock 适配器；288 单测
- 实机核验：`preclose` / `tradestatus` / `isST` / `adjustflag` 全部正确；`volume` 单位 = 股
- 分红字段 4/4 候选名命中

### 2026-09-17 · P0 + P1 规则层

- 包骨架 + 架构守卫；`rules/` 六个模块；171 单测
- 补读 rqalpha 源码，修正 `position_pnl` 语义与送转取整误差
- 修正一字板规则为**非对称**
