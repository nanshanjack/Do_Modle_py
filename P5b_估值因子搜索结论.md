# P5b · 估值因子搜索结论

> 状态：**估值数据管线 + 11 个估值因子 + 58 因子全量搜索完成**
> 结论：**估值因子提供了真实增量（IC_IR 天花板 0.1158 → 0.1325），但三道闸后仍 0 存活**
> 单测：**814 passed**

---

## 一、PIT 安全性验证（**前置门槛，已通过**）

估值因子是否可用，**完全取决于数据源的 PIT 是否正确**。若 baostock 在季末就套用报告期数据，
就是前视，整个估值因子库作废。

### 验证方法

`close / pbMRQ` = 每股净资产（BVPS）。它是**存量**指标，只在**财报公布日**跳变。
若数据源前视，跳变点应**全部落在季末**（03-31 / 06-30 / 09-30 / 12-31）。

### 实测跳变点（sh.601872, 2016–2017）

| 跳变日 | BVPS | 对应报告期 | 是否季末 |
|---|---|---|---|
| 2016-03-24 | 2.5847 | 2015 年报 | ❌ 3 月下旬 |
| 2016-04-22 | 2.6505 | 2016 Q1 | ❌ 4 月下旬 |
| 2016-08-29 | 2.8664 | 2016 中报 | ❌ 8 月底 |
| 2016-10-28 | 2.9210 | 2016 Q3 | ❌ 10 月底 |
| 2017-03-31 | 3.0134 | 2016 年报 | ⚠️ 恰为季末 |
| 2017-04-26 | 3.0567 | 2017 Q1 | ❌ 4 月下旬 |
| 2017-08-30 | 3.0486 | 2017 中报 | ❌ 8 月底 |
| 2017-10-27 | 2.9312 | 2017 Q3 | ❌ 10 月底 |

### 结论

**2016-06-30 / 09-30 / 12-31 全部不跳变**，8 个跳变点中 7 个不在季末。
→ **baostock 按财报公布日更新估值，PIT 安全**。（2017-03-31 落在季末，最可能是招商轮船
2016 年报的实际公布日就是 3/31。）

> **副产品**：`close / pbMRQ` 反推 BVPS 有 ±0.0001 量级的浮点噪声（两列各自独立四舍五入），
> 不影响因子（Spearman 只用秩）。

---

## 二、数据管线改造

| 层 | 改动 |
|---|---|
| `objects.py` | `Bar` 新增 5 个可选字段：`pe_ttm` / `pb_mrq` / `ps_ttm` / `pcf_ncf_ttm` / `turnover` |
| `baostock_src.py` | 这 5 个字段**原本已在 `DAILY_FIELDS` 查询里**，只是被 `row_to_bar` 丢弃 → 补上解析 |
| `cache.py` | **新增 schema 迁移**：`ALTER TABLE ADD COLUMN`，已存在的表自动补列（可空） |
| `sync_data.py` | 落库载荷含估值列 |
| `local_cache.py` | `_opt()` 可空数值解析（缺失/空串/NaN → `None`） |

**设计决策：保留原始倍数（PE/PB），收益率化在因子层做。**
理由：PE 可为负（亏损）、PB 可为负（净资产为负），倍数本身符号混乱；
转成收益率后 `EP = 1/PE` 对负值仍有意义（负 EP = 亏损）。

**数据填充率**：`pe_ttm` / `pb_mrq` / `ps_ttm` / `pcf_ncf_ttm` **100%**，`turnover` 98.9%。
5 只标的共 1871 个交易日为亏损期（`pe_ttm < 0`）—— 这是**真实信息**，不是脏数据。

---

## 三、11 个估值因子

| 因子 | 定义 | 方向 |
|---|---|---|
| `ep_ttm` | `1/peTTM` 盈利收益率 | +1 |
| `bp_mrq` | `1/pbMRQ` 账面市值比 | +1 |
| `sp_ttm` | `1/psTTM` 营收收益率 | +1 |
| `cfp_ttm` | `1/pcfNcfTTM` 现金流收益率 | +1 |
| `turnover_mean_20/60` | 近 N 日平均换手率 | −1（高关注→低收益） |
| `turnover_ratio_20` | 当日换手 / 前 20 日均值 − 1 | −1 |
| `ep_mom_60` / `bp_mom_60` | 收益率的 60 日变化（变便宜） | +1 |
| `ep_vol_60` | EP 的 60 日标准差（估值不确定性） | −1 |
| `ep_pct_250` | 当前 EP 在自身 250 日历史中的分位 | +1 |

**缺失处理**：估值字段为 `None` 时因子返回 `None`（该标的当期退出截面），
**不抛异常、不填 0**。已有专项测试覆盖。

---

## 四、58 因子全量搜索结果

Bonferroni 校正基数 k = **58** → **z = 3.332**（单因子只需 1.96）。

### 闸门汇总

| 闸 | 47 价格/量能 | **+11 估值（58）** | 变化 |
|---|---|---|---|
| ① t 过 Bonferroni | 18/47 | **24/58** | ↑ |
| ② Walk-Forward 稳定 | 0/47 | **0/58** | — |
| ③ 全期 \|IC_IR\| ≥ 0.3 | 0/47 | **0/58** | — |
| **三重全过** | **0** | **0** | — |

### 估值因子的实际表现

| 因子 | 全期 IC_IR | t 值 | 符号一致 | 最差窗 \|IC_IR\| |
|---|---|---|---|---|
| **turnover_mean_20** | **+0.1325** | **+6.65** | 90% | 0.0093 |
| **sp_ttm** | **+0.1217** | **+6.13** | 90% | 0.0083 |
| turnover_mean_60 | +0.1136 | +5.65 | 80% | **0.0233** |
| **bp_mrq** | **+0.1037** | **+5.22** | 80% | 0.0071 |
| cfp_ttm | −0.0922 | −4.64 | 90% | 0.0079 |
| ep_pct_250 | −0.0795 | −3.80 | 80% | 0.0007 |
| bp_mom_60 | +0.0363 | +1.80 | 70% | 0.0036 |
| turnover_ratio_20 | +0.0337 | +1.69 | 80% | 0.0036 |
| ep_ttm | +0.0322 | +1.62 | 50% | 0.0172 |
| ep_vol_60 | −0.0330 | −1.64 | 70% | 0.0054 |
| ep_mom_60 | −0.0197 | −0.98 | 50% | 0.0073 |

### 关键对比：估值 vs 价格/量能

| 指标 | 价格/量能最好 | 估值最好 |
|---|---|---|
| 全期 \|IC_IR\| | 0.1158 (`intraday_range_20`) | **0.1325** (`turnover_mean_20`) |
| t 值 | +5.81 | **+6.65** |
| 最差窗口 \|IC_IR\| | 0.0101 | 0.0093 |

**估值因子确实更强（IC_IR +14%），但幅度有限，且稳定性问题完全一样。**

### maximin 排序（前 6）

| 因子 | 类别 | 全期 IC_IR | **最差窗 \|IC_IR\|** |
|---|---|---|---|
| ma_gap_120 | trend | −0.0804 | **0.0378** |
| vol_60 | volatility | +0.1012 | 0.0358 |
| vol_of_vol_20 | volume | +0.0809 | 0.0337 |
| accel_20_60 | momentum | +0.0318 | 0.0305 |
| **turnover_mean_60** | **valuation** | **+0.1136** | **0.0233** |
| vol_40 | volatility | +0.1020 | 0.0206 |

`turnover_mean_60` 是**唯一进入稳定性前列的新增因子**（估值类）。

---

## 五、方法论反思：门槛是否过严？

**这是本轮最重要的分析。** 必须回答：`IC_IR ≥ 0.3` 这个门槛对 N=8.7 的截面是否合理？

### 理论推导

Spearman IC 在 N 个观测下的零假设标准误 ≈ `1/√(N−1)`。故：

```
IC_IR = mean(IC) / std(IC) ≈ ρ / (1/√(N−1)) = ρ × √(N−1)
```

| N | √(N−1) | IC_IR=0.3 对应的真实 ρ |
|---|---|---|
| **8.7（本板块）** | **2.77** | **0.108** |
| 20 | 4.36 | 0.069 |
| 50 | 7.00 | 0.043 |
| 100 | 9.95 | 0.030 |

**关键结论**：在 N=8.7 下，`IC_IR ≥ 0.3` **恰好等价于经典的「IC ≥ 0.1」经验门槛**
（Grinold-Kahn 的可用因子标准）。

→ **门槛没有过严，标定是正确的。**

### 实测验证

- 观测 `std_IC ≈ 0.44`，理论 `1/√7.7 = 0.36` —— **同量级，验证了推导**
- 实测最大 `|mean_IC|` = 0.056（`turnover_mean_20`）→ 对应真实 ρ ≈ 0.056
- **0.056 < 0.108** → 信号真实存在，但**低于经典可用门槛约一半**

### 结论

> **不是门槛苛刻，是信号确实不够强。**
> 最好的因子（换手率）真实 IC ≈ 0.056，而可用门槛是 0.108。

---

## 六、更根本的约束：截面 N 太小

`std_IC ≈ 1/√(N−1)` 意味着 **N 越小，IC 估计噪声越大**。

| N | IC 估计噪声 | 后果 |
|---|---|---|
| 8.7（本板块） | **0.36** | 真实 ρ=0.05 的因子，单期 IC 在 ±0.36 间跳 |
| 20 | 0.23 | 噪声减半 |
| 100 | 0.10 | 噪声降 3.6 倍 |

**航运板块 N 只有 6–12**，这是**结构性限制**：
- 同行业 → 截面内共同涨跌强 → 有效独立样本更少
- 行业截面**无法剔除行业 beta**（整个截面就是同一行业）

**这不是靠换因子能解决的。**

---

## 七、结论与下一步

### 本轮结论

> **11 个估值因子提供了真实但有限的增量**：IC_IR 天花板从 0.1158 提升到 **0.1325**（+14%），
> 过 Bonferroni 的因子从 18/47 增至 **24/58**。
> **但三道闸后仍 0 存活** —— 所有因子的最差 2 年窗口都塌到 0.04 以下。
>
> 定量判断：最好的因子真实 IC ≈ **0.056**，而 N=8.7 下的可用门槛是 **0.108**。
> **不是门槛苛刻，是信号强度不足。**

### 三条路（需要选择）

| 路径 | 做法 | 代价 |
|---|---|---|
| **A. 接受弱信号，转向组合** | 用 3–5 个方向稳健的低相关因子做**等权合成**，靠分散化把 IC_IR 提上去 | 合成后 IC_IR 提升有限（相关性高）；不解决单因子弱的问题 |
| **B. 扩大截面** | 全市场截面（N>100），噪声降 3.6 倍 | **偏离阶段一单标的交易目标**；需重做数据管线 |
| **C. 换更强信号源** | 运价指数、运力交付、解约事件等**行业特有**信息 | 需新数据源；且 BDTI 已被六次验证无预测力 |

**我的建议：A + C 并行，B 列为 P7 可选。**

理由：
- A 是**零新增依赖**的改进，能把现有 5 个方向稳健的因子利用起来
- C 是唯一可能带来**质变**的路径，但必须先做数据可得性探针
- B 与阶段一的交易目标冲突，且成本最高，不应现在做

### 下一步（P6）

| # | 任务 |
|---|---|
| P6-a | `execution/` RiskGate（9 条规则）+ `OrderManager` 状态机 |
| P6-b | **多因子合成**：用方向稳健的因子（`reversal_5`/`mom_20`/`mom_60`/`ma_gap_20`/`turnover_mean_60`）做等权或 IC 加权合成，检验合成后 IC_IR |
| P6-c | 运价数据可得性探针（**只做探针，不建管线**） |

---

## 八、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | `_ensure_table` 遇已存在的表直接 return → 加列会 INSERT 失败 | 新增 `_migrate_columns`（`ALTER TABLE ADD COLUMN`，可空） |
| 2 | 估值字段**原本已在 `DAILY_FIELDS` 查询里**，只是被 `row_to_bar` 丢弃 | 补上解析，**无需改查询** |
| 3 | 测试构造器未设估值字段 → 11 个估值因子全部返回 `None` | `_bars` 补估值字段；新增 `_bars_no_valuation` 专门测缺失 |
| 4 | `turnover_mean_20` 有 `direction=-1`，测试忘了乘 | 测试区分"原始值"与"方向应用" |
| 5 | `pe_ttm = 0` 会导致 `1/0` 崩溃 | `_yield` 对 `0` 返回 `None` |
