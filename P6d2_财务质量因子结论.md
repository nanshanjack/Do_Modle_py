# P6-d-2 · 财务质量因子结论

> 状态：**财务数据管线 + 8 个质量因子 + 按报告期采样评估完成**
> 结论：**财务因子提供了最高的 IC_IR（0.335 vs 价格类 0.133），但 T=35 太小，统计上无法确认**

---

## 一、数据管线

**6 张表 × 397 行**，12 只标的，`pubDate`（公告日）**100% 非空**。

| 表 | 关键字段 |
|---|---|
| `fin_profit` | `roeAvg`、`npMargin`、`gpMargin` |
| `fin_operation` | `AssetTurnRatio`、`INVTurnRatio`、`NRTurnRatio` |
| `fin_growth` | `YOYNI`、`YOYEquity`、`YOYAsset` |
| `fin_balance` | `liabilityToAsset`、`currentRatio` |
| `fin_cashflow` | `CFOToOR`、`CFOToNP` |
| `fin_dupont` | `dupontROE`、`dupontAssetTurn` |

**抓取过程**：3168 个任务，26.7 分钟，**零失败**（限流友好脚本 `sync_financial.py`）。

---

## 二、8 个财务质量因子

| 因子 | 字段 | 方向先验 |
|---|---|---|
| `fin_roe` | `roeAvg` | +1 |
| `fin_gp_margin` | `gpMargin` | +1 |
| `fin_np_margin` | `npMargin` | +1 |
| `fin_asset_turn` | `AssetTurnRatio` | +1 |
| `fin_liab_to_asset` | `liabilityToAsset` | −1 |
| `fin_yoy_ni` | `YOYNI` | +1 |
| `fin_cfo_to_or` | `CFOToOR` | +1 |
| `fin_dupont_roe` | `dupontROE` | +1 |

**因子库 63 → 71 个 / 13 类别**

---

## 三、PIT 实现

`strategy.panel.attach_financials`：

| 规则 | 实现 |
|---|---|
| **优先用实际公告日** | `pubDate + 1 天`（比「报告期 + 法定滞后」更准确，仍 +1 天保守） |
| 无 `pubDate` 时 | 退回 `statDate + FINANCIAL_LAG_DAYS`（年报 +120 / 半年报 +60 / 季报 +30） |
| 信息衰减 | `max_age_bars=250`（约一年）后置 `None` |

**PIT 专项测试 7 个**，最关键两条：

```python
def test_attach_financials_not_visible_on_pub_date():
    """公告日当天不可见，次日才可见。"""

def test_attach_financials_uses_pubdate_not_statdate():
    """报告期 2025-12-31，但 2026-03-20 才公告 → 3/21 之前都不应有数据。"""
```

---

## 四、⭐ 核心方法论：必须按报告期采样

### 问题

财报因子值在两次公告之间**完全不变**。若按日频算截面 IC：

* 同一份财报被重复计入约 **60 次**
* IC 序列 lag-1 自相关可达 **0.95+**
* 朴素 t 值虚高 **10 倍以上**

### 实现

`evaluation.xsec` 新增两个函数：

| 函数 | 作用 |
|---|---|
| `report_observations(bars, field)` | 找出**字段值发生变化的 Bar 日期** = 新信息首次可见的交易日 |
| `build_report_sections(...)` | 按自然季度分组，**每个标的每季只取一个观测** |

**每个因子用其自己的信息更新事件日作为观测点**（质量因子用财报日，预告因子用公告日）。

---

## 五、结果：日频 vs 报告期双口径

| 因子 | 类别 | 日频 T | 日频 t | 日频膨胀 | **报告 T** | **报告 IC_IR** | **报告 t** |
|---|---|---|---|---|---|---|---|
| **fin_yoy_ni** | quality | 2457 | −1.27 | 2.08 | 35 | **−0.3353** | −1.98 |
| **fin_np_margin** | quality | 2457 | −1.11 | 2.02 | 35 | **−0.3024** | −1.79 |
| fin_gp_margin | quality | 2457 | −1.83 | 2.02 | 35 | −0.2794 | −1.65 |
| fin_liab_to_asset | quality | 2457 | −1.66 | 2.06 | 35 | −0.2700 | −1.60 |
| fin_dupont_roe | quality | 2457 | −0.89 | 2.04 | 35 | −0.2444 | −1.45 |
| fin_roe | quality | 2457 | −0.89 | 2.04 | 35 | −0.2444 | −1.45 |
| fin_cfo_to_or | quality | 2457 | −0.89 | 2.00 | 35 | −0.1902 | −1.07 |
| fin_asset_turn | quality | 2457 | −1.28 | 2.05 | 35 | −0.1667 | −0.99 |
| turnover_mean_20 | valuation | 2517 | +3.30 | 2.02 | 35 | **+0.3801** | +2.25 |
| vol_60 | volatility | 2476 | +2.52 | 2.00 | 35 | +0.3024 | +1.79 |
| mom_60 | momentum | 2476 | −2.01 | 2.04 | 35 | +0.0526 | +0.26 |
| ~~fc_score~~ | forecast | 1701 | −2.08 | 1.96 | **3** | ~~−1.5139~~ | ~~−2.62~~ |

**Bonferroni 阈值 z = 2.733**（候选 8 个财务因子）

### 三个关键发现

**1. 报告期采样的 IC_IR 显著高于日频**

| | 日频 IC_IR 量级 | 报告期 IC_IR |
|---|---|---|
| 财务因子最好 | ~0.09 | **0.335** |
| 价格类最好 | 0.133 | **0.380** |

→ **日频采样会稀释信号**（同一份财报重复 60 次，IC 被平均掉）。
**报告期采样不仅是"避免自相关"，还保留了信号强度。**

**2. 财务因子的 IC 全为负 —— 航运板块的「低质量溢价」**

我设的 direction 都是 +1（高质量看好），实测**全部为负**。

**经济学含义**：周期股在**周期高点**时财务数据最漂亮（高 ROE、高毛利、高增长），
但那时正是**该卖**的时候。财务数据在强周期板块是**反向指标**。

> 这是一个有价值的发现，与 `fc_score` 的「利好出尽」一致 —— 都指向**周期股的"好消息即卖点"**。

**3. `fc_score` 报告期 T = 3 → 结果无意义**

97 个预告观测点按季度分组后，只有 **3 个季度**有 ≥5 只标的。
原因：**预告集中在 1 月和 7 月**（年报预告期 + 半年报预告期），无法按自然季度分组。

→ **该行数据标注为不可评估**，不能作为证据。

---

## 六、结论

> **财务因子提供了目前最高的 IC_IR（`fin_yoy_ni` = 0.335，`fin_np_margin` = 0.302），
> 超过价格类最好水平（0.133）2.5 倍。**
>
> **但报告期采样只有 T = 35 个观测，t 值最大仅 −1.98，远低于 Bonferroni 阈值 2.733。**
> **统计上无法确认。**

### 完整因子库的最终状态

| 信息源 | 因子数 | 报告期/事件采样下的最好 \|IC_IR\| | 是否过门槛 |
|---|---|---|---|
| 价格/量能 | 47 | 0.13（日频） | ❌ |
| 估值 | 11 | 0.38（`turnover_mean_20`，报告期） | ❌ t=2.25 |
| 业绩预告 | 5 | 不可评估（T=3） | ❌ |
| **财务质量** | **8** | **0.34**（`fin_yoy_ni`） | ❌ t=−1.98 |
| **合计** | **71** | — | **0** |

### 为什么都不显著 —— 三重约束

| 约束 | 具体 |
|---|---|
| **1. 截面 N 太小** | 平均 8.7 → IC 估计噪声 0.36（N=100 时仅 0.10） |
| **2. 有效样本数少** | 日频 T=2500 但自相关 0.68 → 有效 620；报告期 T 仅 35 |
| **3. 信号本身弱** | 最好因子真实 IC ≈ 0.056，而可用门槛（N=8.7 下 IC_IR=0.3）对应 IC ≈ 0.108 |

**这三者相乘，使得"在航运板块单行业截面上找到统计显著的因子"几乎不可能。**

---

## 七、下一步

### 已穷尽的方向

| 方向 | 状态 |
|---|---|
| 价格/量能因子 | ✅ 已挖尽（47 个） |
| 估值因子 | ✅ 已挖尽（11 个） |
| 事件因子（预告） | ✅ 已做（5 个，但事件太少） |
| 财务质量因子 | ✅ 已做（8 个，T 太小） |
| 运价数据 | ❌ 设计文档 §2.3 已排除（BDTI 无源） |
| futu-mcp | ❌ 服务不可用 |

### 建议的下一步

| 优先级 | 任务 | 说明 |
|---|---|---|
| **P6-a** | `execution/` RiskGate（9 条规则）+ `OrderManager` | **把工程链路做完**——即使因子弱，系统本身要完整可跑 |
| **P7** | Walk-Forward + Monte Carlo | 鲁棒性验证框架 |
| **P8** | `app/` CLI + 纸面交易 | 可运行系统 |
| 可选 | 扩到全市场截面（N>100） | **偏离单标的交易目标，需用户决策** |

**我的判断**：因子层已经**系统性地穷尽**了。继续找因子的边际收益很低。
**应该把工程链路做完（P6-a → P7 → P8），交付一个完整可运行、结论诚实的系统。**

这本身就是项目定位要求的产出 —— **「首要产出是可信的否定结论，不是能赚钱的信号」**。

---

## 八、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | `sync_financial.py` 把**表名**传给 `getattr(bs, ...)` → 全部 `None` → 3168 任务 2 秒空转且**报"无失败"** | 修接口名映射 + **新增"0 行即 FAIL"校验** |
| 2 | 财务接口名不规则（`query_cash_flow_data` 带下划线） | 显式映射表，不靠拼接 |
| 3 | `fc_score` 用财报日当观测点 → 口径错误 | 改为**每个因子用自己的事件日** |
| 4 | `fc_score` 报告期 T=3 仍输出结果 | 需**标注为不可评估**，不能当证据 |
