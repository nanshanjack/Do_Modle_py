# P2 数据层 · 实现说明

> 对应设计文档 §4.2、§6（P2）
> 状态：**代码与单测已完成（288 passed）**；**实机同步被网络策略阻断，待用户执行**
> 前置：P0 骨架 + P1 规则层（已完成，126 个用例）

---

## 一、交付概览

| 项 | 结果 |
|---|---|
| 测试总数 | **288 passed**（P1 时 171 → 新增 **117**） |
| 数据层测试 | **114**（pit 28 / cache 21 / provider 16 / baostock 适配器 49） |
| 运行耗时 | 0.88s |
| 外部依赖 | `data/` 允许第三方；`rules/` 仍为零依赖（架构断言强制） |
| **实机验证** | ❌ **被环境网络策略阻断**（见 §五） |

---

## 二、目录结构（P2 新增部分）

```
do_modle/
├─ objects.py            [P0]  ← 新增 Bar.is_st 字段
└─ data/                 [P2]
   ├─ __init__.py               # 导出 DataProvider / PITGuard / DataCache
   ├─ provider.py               # DataProvider 抽象 + PROVIDERS 注册表
   ├─ pit.py                    # ★ PITGuard 时点守卫
   ├─ cache.py                  # DataCache（SQLite）
   └─ adapters/
      ├─ __init__.py
      └─ baostock_src.py        # BaostockProvider + 纯解析函数
scripts/
└─ probe_baostock.py            # ★ 实机验证脚本（待用户执行）
tests/
├─ fakes.py                     # FakeProvider（内存数据源，零网络）
├─ test_data_pit.py             # 28
├─ test_data_cache.py           # 21
├─ test_data_provider.py        # 16
└─ test_data_baostock_adapter.py# 49
```

---

## 三、模块职责与依赖

### 3.1 依赖关系

```
objects.py ──┐
numeric.py ──┼──> data/provider.py ──> data/adapters/baostock_src.py
             │            ↑
             ├──> data/pit.py
             └──> data/cache.py
```

`data/` **不依赖** `rules/`、`core/`、`strategy/`。方向单一，架构断言已覆盖。

### 3.2 `provider.py` — 数据源抽象（16 个用例）

| 设计点 | 说明 |
|---|---|
| 三个必须实现 | `get_bars` / `get_instrument` / `get_calendar` |
| 可选接口保守默认 | `get_board` / `get_suspended` / `get_risk_warning` / `get_dividends` 默认返回空，让新数据源先跑起来再补 |
| 注册表 | `@register("name")` → `PROVIDERS`；重名冲突抛错 |
| 会话管理 | `connect` / `disconnect` / 上下文管理器；`BaostockProvider` 保证幂等 |
| **`get_bars` 强制带 `adjust`** | 复权口径是显式参数，不可能"忘记传" |

**验收用例**：注册与创建、未知数据源报错、重名报错、抽象类不可实例化、可选接口默认值、日期区间过滤、复权口径隔离、会话生命周期。

### 3.3 `pit.py` — `PITGuard`（28 个用例）★ 防前视核心

| 数据类型 | 可见时点 | 用例 |
|---|---|---|
| `daily_bar` | `T 15:00` | `test_daily_bar_visible_at_market_close`、`..._not_visible_before_close` |
| `dividend` / `adjust_factor` / `board_membership` / `suspended` / `risk_warning` | `T 09:00` | `test_dividend_visible_from_pre_open` |
| `calendar` | 立即 | `test_calendar_immediately_visible` |
| `shipping_index` | 发布日 `+2 天` | `test_shipping_index_has_two_day_lag` |
| `financial_report` | 报告期末 `+30/60/120` 天 | 参数化 4 例（q1/q3/h1/annual） |
| `express_report` | 公告日 `+1 天` | 默认规则表覆盖 |
| `macro` | 统计期 `+20 天` | 默认规则表覆盖 |

**关键设计**：

1. **`assert_no_lookahead` 抛异常而非静默过滤**——开发期必须炸出来（`test_assert_no_lookahead_raises_on_future_data`）
2. **未知数据类型抛 `KeyError`**——拼错 `"daily_ba"` 不会静默通过（`test_unknown_kind_raises_keyerror`）
3. **`financial_report` 必须传报告类型**——用 `visible_from` 直接问会抛 `ValueError`，逼调用方用 `financial_visible_from(period_end, report_type)`
4. **年报滞后 120 天**——90 天时仍不可见（`test_annual_report_not_visible_90_days_after_period_end`）
5. 行级 `filter` + Bar 专用 `visible_bars` / `assert_no_lookahead_bars`

### 3.4 `cache.py` — `DataCache`（21 个用例）

**存储选型：SQLite（标准库）**，而非 Parquet。设计文档写的是 "Parquet/SQLite"，此处选 SQLite：

| 理由 | 说明 |
|---|---|
| 零额外依赖 | stdlib；Parquet 需 pandas + pyarrow（~50MB） |
| **原生 upsert 语义** | 设计强制要求"增量 upsert 幂等、历史行不被修改"，`INSERT OR REPLACE` 直接满足；Parquet 需全量重写 |
| 规模无差异 | 单标的约 2500 行 |
| SQL 灵活查询 | P5 取因子列时有用 |

**验收用例覆盖的硬要求**：

| 设计硬要求 | 用例 |
|---|---|
| upsert 幂等 | `test_upsert_is_idempotent` |
| 增量更新不修改历史行 | `test_incremental_upsert_does_not_modify_history` |
| 多标的不污染 | `test_multi_symbol_does_not_collide` |
| 同主键覆盖 | `test_upsert_same_key_overwrites_value` |
| 空输入 | `test_upsert_empty_rows_returns_zero` |
| 列不一致报错 | `test_inconsistent_columns_rejected` |
| 主键不在列中报错 | `test_key_not_in_columns_rejected` |
| **SQL 注入防护** | `test_invalid_table_name_rejected` / `test_invalid_column_name_rejected` / `test_invalid_filter_column_rejected` |
| 类型序列化 | date/datetime → ISO、bool → int、None → NULL |

### 3.5 `adapters/baostock_src.py`（49 个用例）

**纯解析函数与网络完全解耦**——49 个用例全部不需要网络。

| 函数 | 职责 | 用例数 |
|---|---|---|
| `infer_board(symbol)` | 代码前缀 → 板块（12 个前缀参数化） | 14 |
| `parse_adjust_flag` / `adjust_flag_to_baostock` | `"1"/"2"/"3"` ↔ `AdjustFlag` | 6 |
| `row_to_bar(row, freq)` | 一行 baostock → `Bar` | 15 |
| `parse_trade_dates(rows)` | 交易日历（仅 `is_trading_day=="1"`） | 3 |
| `parse_stock_basic(row)` | 标的元数据 | 3 |
| `parse_dividend_row(row, symbol)` | 分红送转 → `CorporateAction` | 6 |
| 注册与惰性导入 | `BaostockProvider` 只在 `connect()` 时 import baostock | 3 |

**`row_to_bar` 的两条保守规则**（这是规则层正确性的前提）：

1. 价格字段为空 → 返回 `None`（跳过该日）
2. **`preclose` 为空或 ≤ 0 → 返回 `None`**

第 2 条尤其重要：`preclose` 是涨跌停价的唯一正确基准。若用 `prev_close=0` 继续构造 Bar，`TradabilityGate` 会因 `prev_close <= 0` 返回 `(None, None)` 而**静默关闭涨跌停检查**——回测会虚高。宁可跳过该日，也不接受静默降级。
（用例：`test_row_to_bar_returns_none_when_preclose_missing`）

---

## 四、P1 的连带改动

| 文件 | 改动 | 原因 |
|---|---|---|
| `objects.py` | `Bar` 新增 `is_st: bool = False` | ST 是**逐日**事实（日线自带 `isST` 列），属 PIT 数据，不能放静态的 `Instrument` 上 |
| `rules/tradability.py` | `check(..., is_st: bool \| None = None)`；`None` 时取 `bar.is_st` | 避免为每天单独查一次 ST 名单（2500 天 = 2500 次查询） |
| `tests/test_rules_tradability.py` | +3 个用例 | 覆盖 `bar.is_st` 与显式覆盖的优先级 |

**为什么这是对的**：`Bar` 是"当日市场事实"的载体，`isST` 就是当日事实。把它放 `Bar` 上，规则层只需一个对象就能完成全部判定；同时保留了显式传参覆盖的能力（回测/实盘口径可分离）。

---

## 五、⚠️ 实机验证：被网络策略阻断（**待用户执行**）

### 5.1 诊断结论

| 检查项 | 结果 |
|---|---|
| DNS 解析 `public-api.baostock.com` | ✅ → `114.94.20.42` |
| TCP `:443`（通用出网） | ✅ 可连接 |
| **TCP `public-api.baostock.com:10030`** | ❌ **超时** |
| HTTPS GET 实测（东财接口） | ❌ `HTTP 000`，TLS/HTTP 层被拦截 |

**根因：baostock 走非标准端口 `10030`（原始 TCP socket 协议，非 HTTP），被出网策略阻断。**

这不是代码问题——所有纯解析逻辑已用 49 个用例离线验证。**但以下 4 项仍属未验证状态**，必须由用户在本机执行后确认。

### 5.2 用户执行清单

```bash
PY="C:/Users/Administrator/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"
cd "D:/Codex/Some_project/Do_Modle_py"

# 1) 先做端口连通性检查（8 秒超时）
"$PY" -c "import socket;s=socket.socket();s.settimeout(8);s.connect(('public-api.baostock.com',10030));print('port 10030 OK')"

# 2) 跑完整验证脚本
"$PY" scripts/probe_baostock.py
```

### 5.3 需要确认的 4 件事

| # | 待验证项 | 判据 | 若不符合如何修 |
|---|---|---|---|
| 1 | **端口 10030 是否可通** | 步骤 1 打印 `port 10030 OK` | 若不通 → 换数据源（见 5.4） |
| 2 | **分红接口真实字段名** | 脚本会打印 `[probe] query_dividend_data fields = [...]` | 若与 `_FIELD_CANDIDATES` 不符 → 改 `do_modle/data/adapters/baostock_src.py` 的候选名列表 |
| 3 | **`volume` 单位** | 脚本打印首根 Bar 的 `vol=`，与行情软件对比 | 若为"手"而非"股" → 在 `row_to_bar` 里 ×100 |
| 4 | **复权价差异** | 脚本打印 `hfq 首根 close` vs `none 首根 close`，应不同 | 若相同 → `adjustflag` 参数未生效，检查 `adjust_flag_to_baostock` |

脚本还会顺带验证 `query_suspended_stocks` / `query_stocks_in_risk` 是否可用（不可用只 `[WARN]`，不阻塞）。

### 5.4 端口不通时的备选方案

| 方案 | 协议 | 本环境实测 | 代价 |
|---|---|---|---|
| **baostock** | 私有 TCP :10030 | ❌ 端口不通 | 免费、字段全（含 `preclose`/`tradestatus`/`isST`） |
| **akshare** | HTTPS :443 | ✅ 端口通（HTTP 层被本环境拦截，用户网络应可用） | 免费；**接口不稳定**，需锁版本；运价指数缺 BDTI |
| **tushare pro** | HTTPS :443 | ✅ 端口通 | 需 token + 积分 |
| **本地通达信导出** | 无网络 | ✅ | 需手工导出；字段需自行映射 |

**关键判断**：`akshare` 的 `stock_zh_a_hist` 返回的字段**不含 `preclose` 与 `tradestatus`**，需要用 `close` 自行推算昨收、用成交量为 0 判停牌——**这会削弱涨跌停判定的可靠性**。因此若必须换源，建议 akshare 补 `preclose`（可由前一日 `close` 计算，注意除权日需用复权因子修正）。

**这个风险值得单独登记**：数据源的字段完备性直接决定规则层可信度。

---

## 六、与设计文档的偏差

| # | 设计文档 | 实际实现 | 理由 |
|---|---|---|---|
| 1 | `data/objects.py` 放共享契约 | 已在 P1 移到 `do_modle/objects.py` | 让 `rules` 对 `data` 依赖降为零 |
| 2 | `Instrument.is_st` | 改为 `Bar.is_st` | ST 是逐日 PIT 事实，不是静态元数据 |
| 3 | 缓存 "Parquet/SQLite" | **选 SQLite** | 零依赖 + 原生 upsert 语义（见 §3.4） |
| 4 | 分红字段名未指定 | 用**候选名列表** + `probe_dividend_fields()` 实机探测 | 字段由服务端返回，客户端包内不可查；不能假设 |
| 5 | `yearType` 未指定 | **`"operate"`（除权除息年份）** | 我们按 `ex_date` 过滤，用 `"report"`（预案公告年份）会漏掉跨年除权 |

---

## 七、P2 验收门槛核对

| 门槛（设计文档 §7） | 状态 |
|---|---|
| §4.2 的 8 个 PIT 用例通过 | ✅ 实际 28 个 |
| `as_of` 调早一天时未来数据不可见 | ✅ `test_daily_bar_not_visible_before_close` |
| 增量 upsert 幂等，历史行不被改 | ✅ `test_upsert_is_idempotent` / `test_incremental_upsert_does_not_modify_history` |
| 首次实机任务跑通并落库 | ⏸ **被网络阻断，待用户执行**（§5） |
| 架构断言仍通过 | ✅ 7 个用例 |

**结论**：代码层面 P2 完成；**实机落库这一步卡在环境网络**。P3（`core/` 引擎层）**不依赖实机数据**（可用 `FakeProvider` 驱动），可以先行推进——但建议先确认端口连通性，避免在错误的字段假设上继续盖楼。
