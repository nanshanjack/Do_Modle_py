# P4 评估与鲁棒性层 · 实现说明

> 状态：**基本完成**（perf ✅ pitfalls ✅ P4-0 探针 ✅ reconcile ✅ 全区间校验 ✅）
> 单测：**524 passed**（P4 新增 88）

---

## 一、交付概览

| 项 | 结果 |
|---|---|
| 单测 | **524 passed**（1.6s）/ 无 baostock 521 + 3 skipped |
| 新增模块 | `evaluation/{perf,pitfalls,reconcile}.py` |
| 新增脚本 | `probe_akquant.py` / `validate_full.py` / `reconcile_akquant.py` |
| **全区间 B&H 独立校验** | **残差 −0.0000** |
| **akquant 对账** | **无不一致项**（2 MATCH / 1 已知差异 / 2 SKIP） |
| **抓到的真实 bug** | 1 个（公司行为顺序反了，多派分红 24.80 元） |

---

## 二、`evaluation/perf.py`（29 用例）

**设计取舍：核心指标原生计算，不依赖 quantstats。**

| 理由 | 说明 |
|---|---|
| 可控可测 | 指标标准化，自建约 100 行 |
| **不应进单测路径** | quantstats 拖入 pandas + scipy + matplotlib |
| 口径需求 | 需要**相对基准**指标，quantstats 口径不同 |

quantstats 仅用于 `html()` 生成 tearsheet（可选；未安装时抛 `ImportError`，**不静默降级**）。

**核心语义**：

| 项 | 设计 |
|---|---|
| **基准必填** | `relative()` 无基准直接抛错——不允许只报绝对收益（设计文档 §8.4） |
| **Sortino 无下行风险时返回 `inf`** | 返回 `0.0` 会被误读成"很差"，与事实相反 |
| 年化口径 | 244 交易日/年（可配） |
| 指标 | 总/年化收益、年化波动、Sharpe、Sortino、Calmar、最大回撤及持续天数、日胜率、最好/最差日 |
| 相对指标 | 基准收益、超额收益、跟踪误差、信息比率、Beta、Alpha、**超额 t 值** |

---

## 三、`evaluation/pitfalls.py`（37 用例）

10 项陷阱**可执行断言**，四态：`PASS` / `FAIL` / `WARN` / `SKIP`。

| # | 检查项 | 判定方式 |
|---|---|---|
| 1 | 前视偏差 | 成交是否落在行情日期上；引擎内建 `PITGuard` 断言 |
| 2 | 未来函数 | 无首日成交 + **成交价 = 当日开盘价** |
| 3 | 幸存者偏差 | 最后一根 Bar 距区间末 > 30 天 → FAIL |
| 4 | 样本内外混用 | 未声明 `in_sample_end` → WARN |
| 5 | 多重检验 | `n_trials > 1` 时给出 **Bonferroni 校正阈值** |
| 6 | 成本低估 | 滑点 > 0 且**全部成交含滑点**（成交价 ≠ 开盘价） |
| 7 | 数据窥探 | **恒为 WARN**（需人工确认因子不含"已知结论"） |
| 8 | 回撤期外推 | 区间 ≥ 4 年（油运周期） |
| 9 | 不可交易性 | 无成交发生在停牌日 |
| 10 | 参数敏感性 | **恒为 SKIP**，归属 P7 |

**设计原则**：能自动判定的给 PASS/FAIL；需人工的给 WARN；依赖后续阶段的给 SKIP。
**只报"全部通过"而没有 SKIP 的清单，通常意味着清单本身失效。**

`bonferroni_z(n)` 内置校正表（n=1→1.960, 10→2.807, 100→3.481），log 插值。

---

## 四、P4-0 对账探针（`probe_report.md`）★

**用实证代替推断，修正了 3 处源码推断。**

| # | 我此前的源码推断 | **实测结果** | 修正 |
|---|---|---|---|
| 1 | akquant 不做**停牌**判定 | ❌ **推断有误**。它**会拒绝**，用 **`volume == 0`** 判定（拒因 `not tradable (zero volume, suspension)`） | 停牌**可对账**，但**判据不同** |
| 2 | akquant 不做**涨跌停**判定 | ✅ **确认**。涨停一字板 `status='filled'`, `avg_price=11.0055` —— **照常成交** | 涨跌停**不可对账** |
| 3 | 流动性约束两边一致 | ❌ **语义不同**。自建**当日裁剪+撤销**；akquant **跨日部分成交+顺延** | 不可对账 |
| 4 | （新发现）费用口径 | akquant 把**过户费并入 `commission`**：`2.001 + 0.2001 = 2.2011` ✅ | 可对账，注意口径 |
| 5 | （新发现）`t_plus_one` | **默认 `False`**，必须显式传 `True` | 对账硬要求 |

**场景 D 的 T+1 观测（决定性证据）**：

| 配置 | 持仓 | `available_position` | 判定 |
|---|---|---|---|
| `t_plus_one=True` | 100 | **0** | ✅ T+1 生效 |
| `t_plus_one=False` | 100 | **100** | ❌ 不生效 |

---

## 五、`evaluation/reconcile.py`（24 用例）

**对账范围（实测修正版）**：

| 可对账 7 项 | 不可对账 3 项 |
|---|---|
| 成本 / 滑点 / 撮合价格基准 / tick 对齐 / 手数 / 资金校验 / T+1 | **涨跌停** / **一字板** / **流动性约束** |

**核心约束**：自建先跑 `TradabilityGate` + 流动性裁剪 → **只把已裁剪订单喂给 akquant**，
且 akquant 侧 `volume_limit_pct = 0`（避免二次裁剪）、`t_plus_one = True`。

**四态结论**：`MATCH` / `MISMATCH` / `KNOWN_DIFF` / `SKIP`

**实机对账结果**：

| 场景 | 状态 | 说明 |
|---|---|---|
| 正常买单 1000 股 | **MATCH** | 价 20.01，费用总额 2.201100 —— 一致 |
| 超额订单 5000 股（先裁剪） | **MATCH** | 裁剪到 1000 股后一致 |
| 低价股买单 1000 股 | **KNOWN_DIFF** | 滑点模型差异（见下） |
| 涨停买单 | **SKIP** | 自建拒绝 `LIMIT_UP` |
| 停牌买单 | **SKIP** | 自建拒绝 `SUSPENDED` |

**[PASS] 无不一致项**

### 发现的一个真实模型差异（非 bug）

低价股上：**自建 `6.09` vs akquant `6.08304`**。

| | 做法 | 6.08 元的成交价 |
|---|---|---|
| 自建（模型 B） | 至少 1 tick | **6.09**（tick 对齐） |
| akquant | 纯百分比，**不取整到 tick** | **6.08304**（非 tick 整数倍） |

→ 这是**滑点模型口径差异**，不是 bug。已单列 `KNOWN_DIFF` 状态，
`ok` 判定**不受其影响**（只对 `MISMATCH` 失败）。

> **顺带说明**：akquant 的成交价不是 tick 整数倍，在 A股现实中**不可能成交**。
> 这一点上自建的模型更贴近实际。

---

## 六、全区间 B&H 独立校验（`validate_full.py`）

**独立性的关键**：分红现金与送转股数由脚本**自行重算**，不读取引擎输出。

```
标的       sh.601872 招商轮船
区间       2016-01-01 ~ 2026-09-01   Bar 2591 根   分红 13 条
买入       2016-01-05 开盘 6.2 → 成交 6.21
期末       2026-09-01 收盘 18.39
独立重算   除权后股数 33720（买入 28100，送转 +5620）
           累计分红现金 51,136.3800（13 次除权）
引擎输出   除权后股数 33720，分红现金 51,136.3800
一致性     股数 ✅   分红 ✅

期末净值(回测)      696,726.9849
期末净值(独立)      697,027.1800
差额                    300.1951
  滑点                  281.0000
  费用                   19.1951
  **残差**               -0.0000
```

**绩效**：+248.36% 总收益 / +12.48% 年化 / 34.97% 年化波动 / **−39.47% 最大回撤** / Sharpe 0.357

### 🐛 独立重算抓到的真实 bug

`CorporateActionHandler.apply` 的**顺序反了**——原来先送转、再按送转后股数派息，
**分红基数被放大**。13 次除权累计**多派 24.80 元**。已修正为「先按登记日持仓派息、再送转」。

> **这是"独立重算"的价值**：不能读引擎自己的输出做校验，否则 bug 会被自身的错误掩盖。

---

## 七、验收门槛核对（P4 相关）

| # | 门槛 | 状态 |
|---|---|---|
| 11 | B&H 与手工持有差异完全由成本解释 | ✅ 全区间残差 −0.0000 |
| 12 | akquant 对账差异 < 0.5% 或可解释 | ✅ 无不一致项；1 项已知模型差异已解释 |
| 13 | 10 项陷阱清单全部通过 | ✅ 可执行；7 PASS / 1 WARN / 2 SKIP（诚实标注） |
| 20 | P4-0 对账探针完成，5 场景实际行为已记录 | ✅ `probe_report.md` |

---

## 八、命令速查

```powershell
cd "D:\Codex\Some_project\Do_Modle_py"

.\py.cmd -m pytest                          # 524 passed
.\py.cmd scripts\verify.py                  # 环境 + 单测 + 连通性 + 字段核对
.\py.cmd scripts\sync_data.py               # 数据同步
.\py.cmd scripts\validate_full.py           # 全区间独立校验
.\py.cmd scripts\probe_akquant.py           # P4-0 对账探针 → probe_report.md
.\py.cmd scripts\reconcile_akquant.py       # 对账（可对账范围内一致）
```
