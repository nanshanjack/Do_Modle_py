# P6c · 业绩预告因子结论

> 状态：**业绩预告/快报数据管线 + 5 个事件因子 + 63 因子搜索完成**
> 结论：**找到了真正正交的信息源（平均相关 0.044 vs 其他因子间 0.455），但强度仍不足**

---

## 零、先修正一个决策冲突（**必须先说清**）

用户指示「A + C 并行，B 不做」，其中 C = 换更强信号源。我原本把 C 指向**运价数据**。

**但设计文档 §2.3 已决策排除运价数据**，三条理由：

| # | 理由 |
|---|---|
| 1 | **BDTI（原油运输指数）无可用源** —— akshare 只有 BCTI（成品油）/ BDI（干散），而招商轮船核心资产是 **VLCC 原油船队**，对应 BDTI / VLCC TCE |
| 2 | **PIT 成本高** —— 东方财富二手转载，发布时点与修订规则不透明，任何滞后假设都是猜 |
| 3 | **已六次验证无预测力** —— BDTI/油价对本标的无预测力，是既有结论 |

**但 §2.3 同时批准了一个尚未实现的 C 方向**：

> 阶段一因子只用 **日线量价 + 换手 + 估值 + 日历季节性 + 业绩快报**（全部来自 baostock，PIT 清晰）

其中 **`业绩预告/快报` 与 `日历季节性` 都没做**。而 §2.1 明确标注业绩快报是
「**带发布日，PIT 友好的高频基本面数据**」。

**所以修正后的 C = 业绩预告，而不是运价。** 依据：零新增数据源、PIT 清晰、
与价格/量能/估值正交、且是设计文档已批准的范围。

> **关于日历季节性**：**它不能做截面因子** —— 同一日期所有标的同值，横截面方差为 0。
> 它属于**时序择时层**（判断"当前该不该持仓"），不属于因子层。故本轮不实现，
> 待 P7 择时研究时处理。

---

## 一、数据探针结果（**先探针，不建管线**）

| 标的 | 预告 | 快报 |
|---|---|---|
| sh.601872 招商轮船 | 17 | 1 |
| sh.600026 中远海能 | 22 | 0 |
| sh.601975 招商南油 | 13 | 1 |
| sh.600428 中远海特 | 13 | 19 |
| sh.600798 宁波海运 | 18 | 0 |
| sh.601919 中远海控 | 25 | 2 |
| sh.603565 中谷物流 | 4 | 1 |
| sh.601083 锦江航运 | 7 | 3 |
| sh.603209 兴通股份 | 4 | 2 |
| sz.001205 盛航股份 | 2 | 1 |
| sz.000520 凤凰航运 | 29 | 3 |
| sh.603162 海通发展 | 8 | 0 |
| **落库合计** | **195 行** | |

**关键字段**（`query_forecast_report`）：

| 字段 | 含义 |
|---|---|
| `profitForcastExpPubDate` | **公告日** ← PIT 关键 |
| `profitForcastExpStatDate` | 报告期 |
| `profitForcastType` | 预告类型（预增/预减/略增/首亏/扭亏/…） |
| `profitForcastChgPctUp` / `Dwn` | 净利润同比变动上下限 |

**⚠️ 公告日 ≠ 报告期。** 例：2016-01-23 公告「2015 年净利润 +450%~500%」，
`pub_date=2016-01-23`、`stat_date=2015-12-31`。**用 stat_date 当可见时点就是前视**
（提前 1 个多月知道全年业绩）。

---

## 二、PIT 实现（复用已有规则，零新增）

`data.pit` **已有** `KIND_EXPRESS_REPORT` 规则：

```python
KIND_EXPRESS_REPORT: lambda d: _at(d + timedelta(days=1), _PRE_OPEN)
```

即**公告日次日 09:00 可见**。Bar 是 15:00 收盘，故公告日次日及以后的 Bar 才能看到。

核心函数 `strategy.panel.attach_forecasts`：

* 按 `guard.visible_from(f.pub_date, KIND_EXPRESS_REPORT).date()` 计算可见日
* **最新一条覆盖旧的**（同标的）
* `fc_age_days` = 距首次可见 Bar 的**交易日**间隔
* 超过 `max_age_bars=120` → 置 `None`（退出截面）

> **为什么要有 `max_age_bars`**：业绩预告是**事件驱动**信息。若不设上限，
> "最近一次预告"会永远有效，把事件因子稀释成常数，IC 必然趋近 0。

**PIT 专项测试**（10 个用例）中最关键的两条：

```python
def test_attach_forecast_not_visible_on_pub_date():
    """公告日当天不可见，次日才可见。若当天可见 → 前视。"""

def test_attach_forecast_uses_pub_date_not_stat_date():
    """报告期 2025-12-31，但 2026-03-20 才公告 → 3/21 之前都不应有数据。"""
```

---

## 三、5 个预告因子

| 因子 | 定义 | 方向 |
|---|---|---|
| `fc_score` | 预告类型打分（预增 +1 … 首亏 −1） | +1 |
| `fc_chg_pct` | 净利润同比变动中值（%） | +1 |
| `fc_score_decay` | 类型打分 × `1/(1+age/20)` | +1 |
| `fc_chg_decay` | 变动幅度 × `1/(1+age/20)` | +1 |
| `fc_age` | 距公告交易日数 | −1（越新越好） |

**类型打分表**：预增 +1.0 / 扭亏 +0.75 / 略增 +0.5 / 续盈 +0.25 / 减亏 +0.25 /
不确定 0 / 略减 −0.5 / 增亏 −0.75 / 预减 −1.0 / 首亏 −1.0。
**未知类型返回 `None`（不猜）**。

---

## 四、63 因子搜索结果

Bonferroni 校正基数 k = **63** → **z = 3.354**

### 预告因子表现

| 因子 | 平均 N | mean_IC | **IC_IR** | t 值 | 符号一致 | 最差窗 \|IC_IR\| |
|---|---|---|---|---|---|---|
| **fc_score** | 6.1 | −0.0474 | **−0.0990** | **−4.08** | **90%** | **0.0458** |
| fc_score_decay | 6.1 | −0.0365 | −0.0790 | −3.26 | 90% | 0.0094 |
| fc_chg_pct | 6.4 | −0.0320 | −0.0716 | −3.19 | 80% | 0.0166 |
| fc_chg_decay | 6.4 | −0.0257 | −0.0575 | −2.57 | 70% | 0.0180 |
| fc_age | 6.4 | +0.0086 | +0.0200 | +0.89 | 50% | 0.0154 |

### 两个关键读数

**1. `fc_score` 的 IC 是负的 —— 利好出尽**

我设的方向是 +1（预增看好），实测 **IC_IR = −0.0990**。
即**「预增」公告之后股价反而跌**。这是经典的**利好出尽 / 预期已被 price in**。

**这不是 bug，是有信息量的发现** —— 说明市场在预告公布前已经反应完毕。

**2. `fc_score` 拿到 maximin 第 1 名**

| 排名 | 因子 | 最差窗 \|IC_IR\| |
|---|---|---|
| 1 | **fc_score** | **0.0458** |
| 2 | ma_gap_120 | 0.0378 |
| 3 | vol_60 | 0.0358 |

**`fc_score` 是所有 63 个因子里最稳的**（最差窗口表现最好），超过了全部价格/量能/估值因子。

### 闸门汇总

| 闸 | 47 价格量能 | 58（+估值） | **63（+预告）** |
|---|---|---|---|
| ① t 过 Bonferroni | 18/47 | 24/58 | **25/63** |
| ② Walk-Forward 稳定 | 0/47 | 0/58 | **0/63** |
| ③ 全期 \|IC_IR\| ≥ 0.3 | 0/47 | 0/58 | **0/63** |
| **三重全过** | 0 | 0 | **0** |

---

## 五、⭐ 最重要的发现：预告因子是**真正正交**的

`fc_score` 与其他因子的逐期截面秩相关：

| vs | 相关 | vs | 相关 |
|---|---|---|---|
| turnover_mean_20 | −0.165 | mom_60 | −0.082 |
| sp_ttm | +0.039 | max_ret_60 | +0.015 |
| intraday_range_20 | +0.016 | skew_60 | +0.006 |
| bp_mrq | +0.028 | rev_20 | +0.029 |
| vol_40 | −0.019 | | |

**平均 |相关| ≈ 0.044**

**对比**：价格/量能/估值因子之间的平均 |相关| = **0.455**

> **这是本轮唯一真正的结构性突破**：
> 业绩预告因子提供了**与其他所有因子几乎无关的独立信息**（0.044 vs 0.455，差 10 倍）。
> **"换信息源"的思路被实证验证是正确的。**

### 为什么合成增益仍然有限

理论合成增益：`√(K / (1 + (K−1)·ρ̄))`

加入 `fc_score` 后，ρ̄ 从 0.455 降到约 0.32 → 增益从 1.33× 提到约 1.52×。

但 **`fc_score` 自身只有 0.099，且平均 N 仅 6.1（噪声更大）**：

* 基准仍是 0.1325
* 0.1325 × 1.52 = **0.20** < 0.30

**实测验证**：split-half 合成 OOS

| 方向 | 不含预告 | **含预告** |
|---|---|---|
| 前半→后半 | +0.1546 | +0.1546 |
| 后半→前半 | +0.1072 | **+0.1123** |

`fc_score` 只在后半→前半被选中（训练 IC_IR = −0.1685 → 测试 −0.0365），提升 +5%。

> **正交性是对的，但单个正交源的强度不够。** 需要**更多**同类正交源累积。

---

## 六、结论

> **业绩预告提供了真正正交的信息（平均相关 0.044 vs 其他因子 0.455），
> 且 `fc_score` 是所有 63 个因子里最稳的（maximin 0.0458）。
> 但强度不足（IC_IR 0.099），三道闸后仍 0 存活。**
>
> **「换信息源」的方向被验证正确，但需要累积更多正交源。**

### 三条路径的更新

| 路径 | 状态 |
|---|---|
| 单因子更强（IC_IR ≥ 0.226） | 需**更多**正交源累积 |
| 因子不相关（ρ̄ → 0） | ✅ **已验证可达**（预告因子 ρ≈0.044） |
| 扩大截面（N → 100） | 不做（用户已决策） |

**关键认识**：路径 2 已被验证**可行**（预告因子证明正交源存在）。
若再找到 3–5 个类似正交源，理论增益可从 1.33× 提到 `√(8/(1+7×0.1)) ≈ 2.6×`，
则 0.1325 × 2.6 = **0.34 > 0.3** —— **跨过门槛是可能的**。

**所以下一步不是"放弃"，而是"找更多正交源"。**

---

## 七、候选正交源（P6-d 待探针）

| 源 | 类型 | 与价格/估值正交性 | 数据可得性 |
|---|---|---|---|
| **业绩快报**（已落库 33 条） | 事件 | 高 | ✅ 已有 |
| **分红预案 / 分红实施** | 事件 | 高 | ✅ dividend 表已有 |
| **解禁** | 事件 | 高 | 需探针 |
| **运力交付 / 拆解** | 供给侧 | 高 | 需探针 |
| **股东户数变化** | 筹码 | 高 | 需探针 |
| **大宗交易 / 龙虎榜** | 资金 | 中 | 需探针 |
| **分析师评级变动** | 预期 | 中高 | futu-mcp 有 `quote_research_rating_summary` |

> ⚠️ **纪律提醒**：每个新源都必须走**同一套三重闸**（Bonferroni 基数随候选数增长），
> 且必须**先做数据可得性探针**，不建空管线。

---

## 八、交付

| 模块 | 内容 |
|---|---|
| `objects.py` | `Forecast` 数据类 + `FORECAST_KIND_SCORE` + `forecast_kind_score()` |
| `objects.py` | `Bar` 新增 3 个字段：`fc_score` / `fc_chg_pct` / `fc_age_days` |
| `baostock_src.py` | `get_forecasts()` + `parse_forecast_row()` + `parse_express_row()` |
| `local_cache.py` | `get_forecasts()` |
| `sync_data.py` | 落库 `forecast` 表（**195 行**） |
| `strategy/panel.py` | **`attach_forecasts()`** —— PIT 核心（可见日 + 信息衰减） |
| `strategy/factors.py` | 5 个预告因子（因子库 58 → **63**） |

**PIT 规则零新增**：复用已有的 `KIND_EXPRESS_REPORT`（公告日 +1 天 09:00）。

单测：**863 passed**（P6c 新增 27，含 10 个 `attach_forecasts` 专项）

---

## 九、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | `sorted((date, Forecast))` 在日期相同时比较 `Forecast` 对象 → `TypeError` | 改用 `key=lambda pair: pair[0]` |
| 2 | `local_cache.py` 未导入 `Forecast` | 补导入 |
| 3 | 脚本批量替换时 `forecasts = ...` 与 `attach_forecasts(...)` 各被插入两次 | 去重 |
| 4 | 测试用 `_bars`（已预设 fc 字段）测 `attach_forecasts` → 断言失效 | 改用 `_bars_no_valuation`（fc 全 None） |
