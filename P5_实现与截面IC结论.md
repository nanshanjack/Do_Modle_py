# P5 因子层 · 实现与因子搜索结果

> 状态：**因子库扩充到 47 个 + 系统化搜索完成**；**结论：三道闸后无因子存活**
> 单测：**748 passed**（P5 新增 224）

---

## 零、因子搜索：为什么不能"挑最好的那个"

设候选因子数 ``k``。若从中挑 IC_IR 最大的：

| k | 纯噪声下"至少一个显著(p<0.05)"的概率 = 1 − 0.95^k |
|---|---|
| 8 | 34% |
| 20 | 64% |
| **47** | **90%** |

**即：不管数据里有没有信号，"挑最好"这个规则一定能挑出一个"显著"的因子。**

所以搜索必须**做过滤，不做挑选**，且校正基数 = **实际尝试的候选数**：

| 闸 | 内容 |
|---|---|
| ① | 全空间 Bonferroni：``z = Φ⁻¹(1 − 0.025/k)``，k=47 → **z = 3.273** |
| ② | Walk-Forward 稳定性：**每个**滚动窗口 ``|IC_IR| ≥ 0.10`` 且符号一致率 ≥ 80% |
| ③ | 全期经济意义：``|IC_IR| ≥ 0.3`` |

排序采用 **maximin（按最差窗口）**，而非按全期最大——后者就是"挑最好"。

---

## 一、因子库：47 个，10 个类别

| 类别 | 数量 | 因子 |
|---|---|---|
| momentum | 10 | `mom_5/10/20/40/60/90/120/180/250`、`accel_20_60` |
| trend | 10 | `ma_gap_5/10/20/60/120`、`price_pos_20/60/120/250`、`high_prox_250` |
| volatility | 7 | `vol_10/20/40/60/120`、`downside_ratio_60`、`intraday_range_20` |
| volume | 6 | `vratio_5/20/60`、`vol_of_vol_20`、`vp_corr_20`、`turnover_trend_20` |
| reversal | 5 | `rev_1/3/5/10/20` |
| overbought | 3 | `rsi_6/14/24` |
| lottery | 2 | `max_ret_20/60` |
| pattern | 2 | `consec_up`、`gap_freq_20` |
| higher_moment | 1 | `skew_60` |
| liquidity | 1 | `amihud_20` |

**性能优化**：`FactorSpec.min_bars` 定义为**该因子恰好需要的 K 线数**，
`panel.py` 据此精确切片，把原先的 O(n²) 列表拷贝降为 O(1)。
47 因子 × 12 标的 × 2586 天全量计算约 90 秒。

---

## 二、搜索结果：三道闸后**无因子存活**

| 闸 | 通过数 |
|---|---|
| ① t 值过 Bonferroni (z=3.273) | **18/47** |
| ② Walk-Forward 稳定（每窗 \|IC_IR\|≥0.10 且符号一致≥80%） | **0/47** |
| ③ 全期 \|IC_IR\| ≥ 0.3 | **0/47** |
| **三重全过** | **无** |

### 按「最差窗口 |IC_IR|」排序（maximin，最诚实）

| 因子 | 全期 IC_IR | **最差窗 \|IC_IR\|** | 最差窗口 | 符号一致 |
|---|---|---|---|---|
| ma_gap_120 | −0.0804 | **0.0378** | W2 (2016-12~2018-12) | 90% |
| vol_60 | +0.1012 | 0.0358 | W6 (2020-12~2022-12) | 80% |
| vol_of_vol_20 | +0.0809 | 0.0337 | W2 (2016-12~2018-12) | 80% |
| accel_20_60 | +0.0318 | 0.0305 | W5 (2019-12~2021-12) | 70% |
| vol_40 | +0.1020 | 0.0206 | W5 (2019-12~2021-12) | 90% |
| ma_gap_20 | −0.0746 | 0.0189 | W5 (2019-12~2021-12) | 100% |
| mom_60 | −0.0822 | 0.0176 | W1 (2016-01~2017-12) | 90% |

**关键读数**：

1. **表现最"稳"的 `ma_gap_120`，最差窗口也只有 0.0378** —— 比门槛 0.3 低一个数量级
2. **全期 |IC_IR| 最高的 `intraday_range_20`（0.1158, t=5.81, 符号一致 100%）
   在最差窗口塌到 0.0101** —— 几乎完全失效
3. **18 个因子全期统计显著，但没有一个能在所有滚动窗口存活**

> **这就是"不挑最好"规则下的真实图景**：全期显著的因子，在某个 2 年窗口里就是噪声。
> 若按"挑 IC_IR 最大"选 `intraday_range_20`，会得到一个在特定时期完全失效的策略。

### 各类别内 |IC_IR| 最大（**仅供参考，不代表可用**）

| 类别 | 因子 | IC_IR | t 值 | 符号一致 |
|---|---|---|---|---|
| volatility | intraday_range_20 | +0.1158 | +5.81 | 100% |
| lottery | max_ret_60 | +0.1086 | +5.40 | 80% |
| trend | ma_gap_60 | −0.0906 | −4.51 | 90% |
| pattern | gap_freq_20 | +0.0855 | +4.28 | 100% |
| momentum | mom_60 | −0.0822 | −4.09 | 90% |
| volume | vol_of_vol_20 | +0.0809 | +4.06 | 80% |
| reversal | rev_20 | +0.0755 | +3.79 | 100% |
| higher_moment | skew_60 | +0.0715 | +3.56 | 80% |
| overbought | rsi_24 | +0.0640 | +3.21 | 80% |
| liquidity | amihud_20 | +0.0435 | +2.18 | 80% |

---

## 三、结论

> **在航运板块截面（平均 N=8.7）上，47 个价格/量能类因子，三道闸后无因子存活。**
>
> 18 个因子全期统计显著（t 值过 Bonferroni z=3.273），
> **但没有一个能在全部 10 个 Walk-Forward 滚动窗口中都保持 |IC_IR| ≥ 0.10**。
> 表现最稳的 `ma_gap_120`，最差窗口仅 0.0378 —— 比经济意义门槛低一个数量级。

**这个结论的可信度依据**：
1. 校正基数 = 实际候选数 47（不是事后挑选的 8）
2. Walk-Forward 多窗口交叉验证（不是单次切分）
3. 排序用 maximin（不是"挑最好"）

**实践含义**：

| ❌ 不要做 | ✅ 应该做 |
|---|---|
| 继续在价格/量能上换参数 | 找**与价格/量能正交**的新信息源 |
| 按全期 IC_IR 挑最大 | 按最差窗口排序 |
| 单次样本内外切分 | Walk-Forward 多窗口 |

**下一步方向**：运价指数（BDI/BDTI/CCFI）、估值（PE/PB/EV-EBITDA）、
事件（分红、解禁、运力交付）—— 这些与价格/量能正交，才可能带来**新的**信息。

> ⚠️ **注意**：项目历史结论已六次验证 **BDTI/油价无预测力**。
> 若要用运价，必须是**更细分的航线运价**或**运价与股价的背离**，
> 且必须走同一套三重闸 —— 不能因为"这次不一样"就放低标准。

---

## 四、分层检验（`--split` / `--stratify`）

### 4.1 样本内 / 样本外

| | 样本内（N=6.7） | 样本外（N=11.2） |
|---|---|---|
| 过三重门槛 | 0/8 | 0/8 |
| **符号一致** | — | **7/8**（随机概率 0.78%） |

仅 `mom_120` 翻转。

### 4.2 市场状态分层（601872 60 日动量，up=1301 / down=1230 天）

**8/8 符号一致**（随机概率 0.39%）。

| 因子 | up IC_IR | down IC_IR | 强度比 | 判定 |
|---|---|---|---|---|
| reversal_5 | +0.0719 | +0.0631 | 1.1 | ✅ 稳健 |
| mom_20 | −0.0663 | −0.0780 | 1.2 | ✅ 稳健 |
| mom_60 | −0.1010 | −0.0628 | 1.6 | ✅ 稳健 |
| ma_gap_20 | −0.0490 | −0.0953 | 1.9 | ✅ 稳健 |
| volume_ratio_20 | −0.0208 | −0.0096 | 2.2 | ✅ 稳健 |
| rsi_14 | +0.0080 | +0.0402 | 5.0 | ⚠️ 敏感 |
| mom_120 | −0.0195 | −0.0015 | 13.1 | ⚠️ 敏感 |
| vol_20 | +0.1304 | +0.0047 | 27.6 | ⚠️ 敏感 |

### 4.3 三档判据

| 现象 | 判定 |
|---|---|
| 方向一致 + 强度不足 | **有真实但微弱的信号** |
| 方向不一致 | 噪声 |
| 强度比 > 5 | 对市况/时期敏感，不可用 |

**方向是真实的**（8 因子 4 切分符号全一致，随机概率 0.39%），**但强度不足**。
这与搜索结论一致：**信号方向对，但被噪声淹没，且在部分时期完全消失**。

---

## 五、交付

| 模块 | 内容 |
|---|---|
| `do_modle/strategy/factors.py` | **47 个因子**（参数化生成）+ `FactorSpec` + `factor_categories` |
| `do_modle/strategy/panel.py` | 面板构建（**O(1) 精确切片** + PIT 守卫 + 停牌跳过） |
| `do_modle/evaluation/xsec.py` | 截面 IC + 分层 + **Walk-Forward**（`rolling_windows` / `evaluate_stability`） |
| `scripts/xsec_ic.py` | 截面 IC 报告（`--split` / `--stratify`） |
| `scripts/factor_search.py` | **系统化搜索**（三重闸 + maximin 排序） |
| `config/universe_shipping.yaml` | 航运板块 12 只研究池 |

---

## 六、命令速查

```powershell
cd "D:\Codex\Some_project\Do_Modle_py"

.\py.cmd -m pytest                          # 748 passed
.\py.cmd scripts\xsec_ic.py                 # 基线截面 IC
.\py.cmd scripts\xsec_ic.py --split 2021-12-31    # 样本内外
.\py.cmd scripts\xsec_ic.py --stratify            # 市场状态分层
.\py.cmd scripts\factor_search.py           # 47 因子系统化搜索
```

---

## 七、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | baostock 长连接批量查询断线，首轮只成功 4/12 | `sync_data.py` 加**断线重连 + 3 次重试** → 12/12 |
| 2 | `ICSeries.ic_ir` 在等值序列上爆到 5.9e15（std 有 ~1e-17 浮点残差） | 加 `_STD_EPSILON = 1e-12` |
| 3 | 闸门 ② 与 ③ 用了相同阈值 0.3 → ③ 冗余 | ② 改为 `MIN_WINDOW_IC_IR = 0.10`，只管稳定性 |
| 4 | 测试忘了 `direction` 乘数 | 把"原始值语义"与"方向应用"分开测 |
| 5 | 测试数据窗口内 IC 无方差 → `std=0` → `IC_IR=nan` | 这是**正确行为**；改用 `_section_perm` 构造可控 IC |
| 6 | 因子库重构后测试直接 import 已删除的模块级函数 | 改用 `FACTORS[name].fn` |
| 7 | `_bars` 未设 `amount` → `amihud` 恒为 None | 测试构造器补 `amount` 与成交量波动 |
| 8 | `factor_search.py` 字符串内嵌双引号 → SyntaxError | 改用中文引号 |


---

## 一、为什么必须用板块截面 IC（用户明确要求）

单标的时序 IC 的致命问题：**每个时点只有 1 个观测**。

| N（每期截面标的数） | 随机因子 IC 的噪声下界 = 1/√N |
|---|---|
| 1 | **1.000** |
| 4 | 0.500 |
| 6 | 0.408 |
| 10 | 0.316 |
| 12 | 0.289 |

N=1 时噪声下界是 **1.0**——意味着**随机数的"IC"可以达到 ±1**。前六轮"BDTI/油价无预测力"的结论，
在单标的时序口径下**统计功效极低，不足以支撑否定结论**。

**N ≥ 10 才真正降低噪声**（N=4 的 0.500 比 N=10 的 0.316 差 58%）。

**架构影响：零。** 研究轨只是"多读几个 symbol 的数据、算截面 IC"，不进入交易链路。
`Portfolio.books` 已是 `dict[str, PositionBook]`；`DataProvider.get_bars(symbol, ...)` 已带 `symbol`。

---

## 二、交付

| 模块 | 内容 |
|---|---|
| `do_modle/evaluation/xsec.py` | `CrossSection` / `ICSeries` / `spearman` / `build_cross_sections` / `compute_ic_series`（32 用例） |
| `do_modle/strategy/factors.py` | 8 个因子（纯函数，PIT 安全）+ `FactorSpec` 注册表 |
| `do_modle/strategy/panel.py` | 因子面板 / 收益面板构建（PIT 守卫） |
| `config/universe_shipping.yaml` | 航运板块 12 只研究池 |
| `scripts/xsec_ic.py` | 截面 IC 报告（含三重门槛判读） |

**数据**：12 只标的 **22,703 根 K 线**入库（2016-01-04 ~ 2026-09-01）

| 标的 | 名称 | 细分 | K 线数 | 起始 |
|---|---|---|---|---|
| sh.601872 | 招商轮船 | 原油+干散 | 2591 | 2016-01-04 |
| sh.600026 | 中远海能 | 原油+LNG | 2591 | 2016-01-04 |
| sh.601975 | 招商南油 | 成品油 | 1856 | 2019-01-08 |
| sh.600428 | 中远海特 | 特种船 | 2591 | 2016-01-04 |
| sh.600798 | 宁波海运 | 干散 | 2591 | 2016-01-04 |
| sh.601919 | 中远海控 | 集运 | 2591 | 2016-01-04 |
| sh.603565 | 中谷物流 | 内贸集运 | 1437 | 2020-09-25 |
| sh.601083 | 锦江航运 | 集运 | 665 | 2023-12-05 |
| sh.603209 | 兴通股份 | 液体化学品 | 1078 | 2022-03-24 |
| sz.001205 | 盛航股份 | 液体化学品 | 1289 | 2021-05-13 |
| sz.000520 | 凤凰航运 | 干散 | 2591 | 2016-01-04 |
| sh.603162 | 海通发展 | 干散 | 832 | 2023-03-29 |

---

## 三、⚠️ 首轮截面 IC 结论：**8 个因子全部未通过三重门槛**

区间 2016-01-01 ~ 2026-09-01，前向收益 5 日，入场 t+1 开盘：

| 因子 | 类别 | 期数 | 平均N | 噪声下界 | mean_IC | std_IC | **IC_IR** | t值 | IC>0 |
|---|---|---|---|---|---|---|---|---|---|
| ma_gap_20 | trend | 2517 | 8.7 | 0.339 | −0.0328 | 0.4396 | **−0.0746** | −3.74 | 45.9% |
| mom_120 | momentum | 2416 | 8.6 | 0.341 | −0.0050 | 0.4659 | **−0.0107** | −0.53 | 49.3% |
| mom_20 | momentum | 2516 | 8.7 | 0.339 | −0.0333 | 0.4414 | **−0.0755** | −3.78 | 46.0% |
| mom_60 | momentum | 2476 | 8.7 | 0.340 | −0.0366 | 0.4448 | **−0.0822** | −4.09 | 46.0% |
| reversal_5 | reversal | 2531 | 8.7 | 0.338 | +0.0298 | 0.4440 | **+0.0670** | +3.37 | 52.3% |
| rsi_14 | overbought | 2522 | 8.7 | 0.338 | +0.0108 | 0.4263 | **+0.0254** | +1.28 | 50.5% |
| vol_20 | volatility | 2516 | 8.7 | 0.339 | +0.0296 | 0.4407 | **+0.0671** | +3.37 | 53.6% |
| volume_ratio_20 | volume | 2515 | 8.7 | 0.339 | −0.0065 | 0.4138 | **−0.0157** | −0.79 | 49.1% |

### 三重门槛结果

| 门槛 | 通过数 |
|---|---|
| ① `\|mean_IC\|` > 噪声下界（0.339） | **0/8** |
| ② `\|t\|` ≥ Bonferroni 阈值 2.733 | 5/8 |
| ③ `\|IC_IR\|` ≥ 0.3（经济意义） | **0/8** |
| **三重全过** | **无** |

### 三个关键读数

**1. IC 比噪声小一个数量级。** 最大 `|mean_IC|` = 0.0366，噪声下界 0.339 —— **差 9 倍**。
因子信号被噪声完全淹没。

**2. t 值显著 ≠ 经济有意义。** 5 个因子过了 Bonferroni（t 从 3.37 到 −4.09），
但 IC_IR 只有 0.067~0.082。原因：**期数 2400+ 时，t = IC_IR × √T，IC_IR=0.08 就能得到 t≈4**。

> **这是最容易误读的陷阱**：只看 t 值会得出"mom_60 显著有效"的结论，
> 但 IC_IR=0.08 意味着每期 IC 的均值只占其波动的 8% —— **实际不可用**。
> 故 `xsec_ic.py` 强制三重门槛，把 t 值单独列出但不作为唯一判据。

**3. 动量因子 IC 全为负 → 航运板块是反转板块。**
`mom_20/60/120` 与 `ma_gap_20` 的 IC 均为负，`reversal_5` 为正。
方向一致且符合航运强周期的直觉：**涨多了就跌**。

### 截面 N 的时间分布（重要限制）

| 年份 | 截面可用标的数 | 噪声下界 |
|---|---|---|
| 2016 | 6 | 0.408 |
| 2019 | 7 | 0.378 |
| 2021 | 9 | 0.333 |
| 2023 | 11 | 0.302 |
| 2025 | 12 | 0.289 |

**5 只标的上市较晚**（2019–2023），故早期截面 N 只有 6–7，**统计功效比后期更弱**。
报告必须给出**平均 N**（8.7），不能直接用 12 —— 这正是 `xsec_ic.py` 修正的一个报告 bug。

---

## 四、PIT 安全设计

**最关键的人工防线是一组测试**：追加未来数据后，历史因子值必须**逐点不变**。

```python
@pytest.mark.parametrize("name", sorted(FACTORS))
def test_factor_is_pit_safe_under_appended_future(name):
    full = _rising(120)
    for cut in (30, 60, 90):
        short = _bars(full[:cut])
        long = _bars(full)                      # 追加了未来
        assert compute_factor(name, short) == approx(compute_factor(name, long[:cut]))
```

若某因子偷偷用了全序列统计量（如"全期均值"），这个测试会失败。

**其他 PIT 措施**：
- 因子只用 `bars[:i+1]`（面板构建时切片）
- 因子用**后复权价**（前复权会重算历史 → 前视）
- 收益用**不复权价**（避免除权假跳空），入场 **t+1 开盘**（与撮合口径一致）
- 停牌日不产生因子值也不产生收益

---

## 五、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | baostock 长连接批量查询会断（`WinError 10053` / `10002007`），首轮只成功 4/12 | `sync_data.py` 加**断线重连 + 3 次重试** → 12/12 |
| 2 | `ICSeries.ic_ir` 在等值序列上爆到 5.9e15（std 有 ~1e-17 浮点残差） | 加 `_STD_EPSILON = 1e-12`，退化时返回 `nan` |
| 3 | `xsec_ic.py` 用 N=12 算噪声下界，实际平均 N 只有 8.7 | 改用各因子**实际平均 N** 的 `noise_floor` |
| 4 | 测试忘了 `direction` 乘数（`vol_20`/`rsi_14` 的 direction = −1） | 把"原始值语义"与"方向应用"**分开测** |
| 5 | `sz.000520` 数据源返回"凤凰航运"（已改名，非"长航凤凰"） | 以数据源为准 |

---

## 六、分层检验：**方向稳健，强度不足**

### 6.1 样本内 / 样本外（`--split 2021-12-31`）

| 因子 | 全期 IC_IR | 样本外 IC_IR | 符号 |
|---|---|---|---|
| ma_gap_20 | −0.0746 | −0.0663 | ✅ 同号 |
| mom_20 | −0.0755 | −0.0478 | ✅ 同号 |
| mom_60 | −0.0822 | **−0.1253** | ✅ 同号 |
| reversal_5 | +0.0670 | **+0.0837** | ✅ 同号 |
| rsi_14 | +0.0254 | +0.0466 | ✅ 同号 |
| vol_20 | +0.0671 | **+0.1176** | ✅ 同号 |
| volume_ratio_20 | −0.0157 | −0.0008 | ✅ 同号 |
| mom_120 | −0.0107 | +0.0012 | ❌ **符号翻转** |

- 样本内（N=6.7）：0/8 过三重门槛
- 样本外（N=11.2）：0/8 过三重门槛
- **7/8 符号一致** —— 随机下概率仅 0.78%

### 6.2 市场状态分层（`--stratify`，601872 60 日动量）

up=1301 天，down=1230 天。**8/8 全部同号**（随机下概率 0.39%）。

| 因子 | up IC_IR | down IC_IR | 强度比 | 判定 |
|---|---|---|---|---|
| reversal_5 | +0.0719 | +0.0631 | 1.1 | ✅ **稳健** |
| mom_20 | −0.0663 | −0.0780 | 1.2 | ✅ **稳健** |
| mom_60 | −0.1010 | −0.0628 | 1.6 | ✅ **稳健** |
| ma_gap_20 | −0.0490 | −0.0953 | 1.9 | ✅ **稳健** |
| volume_ratio_20 | −0.0208 | −0.0096 | 2.2 | ✅ **稳健** |
| rsi_14 | +0.0080 | +0.0402 | 5.0 | ⚠️ 对市况敏感 |
| mom_120 | −0.0195 | −0.0015 | 13.1 | ⚠️ 对市况敏感 |
| vol_20 | **+0.1304** | +0.0047 | **27.6** | ⚠️ 对市况敏感 |

### 6.3 结论：三档判据

| 现象 | 判定 |
|---|---|
| 方向一致 + 强度不足 | **有真实但微弱的信号** |
| 方向不一致 | 噪声 |
| 强度比 > 5 | 对市况/时期敏感，不可用 |

**本轮最终结论**：

> **5 个因子方向稳健**（`reversal_5` / `mom_20` / `mom_60` / `ma_gap_20` / `volume_ratio_20`），
> **但不是噪声** —— 8 因子在 4 种切分下符号全一致，随机概率仅 0.39%。
> **然而强度全部不足**：|IC_IR| 最大 0.101（`mom_60` 在上涨市况），远低于经验门槛 0.3。
>
> **`vol_20` 表面最强（up 市况 0.1304）但对市况极度敏感（强度比 27.6 倍）→ 不可用。**

**这个结论的价值**：它把"全部无效"细化为"**方向真实、强度不足、部分因子对市况敏感**"。
前者会导致放弃正确方向；后者指明了正确方向——**若要提升强度，应找与这些方向正交的新信息源
（运价、估值、事件），而不是继续在价格/量能上换参数**。

---

## 七、结论与下一步

**本轮结论（可信的否定，且比前六轮精细）**：

> 航运板块截面（平均 N=8.7）上，8 个价格/量能因子**方向一致且非噪声**，
> 但 **|IC_IR| 全部 ≤ 0.13**，远低于 0.3 门槛；其中 3 个对市况敏感。
> **当前因子库在航运板块上没有可用信号。**

**这个结论比前六轮更可信**，因为统计功效从 N=1 提升到 N=8.7（噪声下界 1.0 → 0.339），
且新增了**符号一致性**与**强度一致性**两重交叉验证。

**下一步（P5 剩余）**：

| # | 任务 | 说明 |
|---|---|---|
| P5-a | ✅ **已完成** | 分层报告（样本内外 + 市场状态） |
| P5-b | 样本外一致性深化 | Walk-Forward 多滚动段（当前只有 1 次切分） |
| P5-c | 因子扩充 | 估值（PE/PB）、事件、季节性 —— **需新数据源字段**；方向应选与价格/量能正交的信息 |
| P5-d | `SignalGenerator` + `PositionSizer` | 禁止主动拆单；差额 <100 股不下单 |
