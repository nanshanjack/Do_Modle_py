# P2 数据层 + P3 引擎层 · 实现与验证说明

> 状态：**代码完成，410 个单测全绿，实机验证通过**
> 佣金前提：万五（0.0005）+ 免五（`min_commission = 0.0`）

---

## 一、交付概览

| 项 | 结果 |
|---|---|
| 测试总数 | **431 passed**（隔离环境，含 baostock） |
| 无 baostock 环境 | **428 passed, 3 skipped**（可选依赖缺失自动跳过） |
| 运行耗时 | 1.7s |
| **实机数据同步** | ✅ **2591 个交易日落库**（2016-01-04 ~ 2026-09-01） |
| **B&H 成本校验（真实数据）** | ✅ **残差 0.0000**（无分红区间 + 含分红区间均通过） |
| 佣金费率 | **万一（0.0001）+ 免五** |
| 滑点模型 | **至少 1 tick**（模型 B） |
| `rules/` 零第三方依赖 | ✅ 架构断言强制 |

---

## 二、你现在怎么操作来验证

> ⚠️ **注意 shell 语法不同**。下面以 **PowerShell** 为主（Windows 默认）。
> bash / Git Bash 用文末的「bash 写法」。

### 一键验证（推荐）

```powershell
# PowerShell
cd "D:\Codex\Some_project\Do_Modle_py"

# 方式 A：用启动器（最短，免记长路径）
.\py.cmd scripts\verify.py
.\py.cmd scripts\verify.py --skip-network    # 只跑单测，不联网

# 方式 B：直连 Python（路径含空格时必须用 & 调用运算符）
$PY = "C:\Users\Administrator\.workbuddy-ai\binaries\python\envs\default\Scripts\python.exe"
& $PY scripts\verify.py
& $PY scripts\verify.py --skip-network
```

**实测结果：通过 20 · 失败 0 · 警告 0**（端口恢复后）

### PowerShell 常见错误（对照）

| ❌ 错误写法 | 报错 | ✅ 正确写法 |
|---|---|---|
| `PY="C:\...\python.exe"` | `The term 'PY=...' is not recognized` | `$PY = "C:\...\python.exe"`（PowerShell 变量要 `$`，赋值不能贴在一起） |
| `"$PY" scripts\verify.py` | `ParserError: Unexpected token` | `& $PY scripts\verify.py`（字符串不能直接当命令，要 `&` 调用运算符） |
| `$PY scripts\verify.py` | `ParserError` | 同上，或直接用 `.\py.cmd scripts\verify.py` |

### 分步操作

| 目的 | PowerShell 命令 | 预期 |
|---|---|---|
| 跑单测 | `.\py.cmd -m pytest` | `411 passed` |
| 跑单个文件 | `.\py.cmd -m pytest tests\test_rules_position.py -v` | 27 passed |
| 数据源连通 + 字段核对 | `.\py.cmd scripts\verify.py` | 全部 `[PASS]` |
| **同步真实数据落库** | `.\py.cmd scripts\sync_data.py` | 2591 行 × 5 张表 |
| **B&H 成本校验** | `.\py.cmd scripts\validate_bh.py` | 残差 `0.0000` |

> **用错 Python 会怎样**：`verify.py` 会自检解释器并打印修复方式；
> 单测在无 baostock 的环境显示 `408 passed, 3 skipped`（可选依赖自动跳过），
> 不会出现莫名其妙的失败。

### bash / Git Bash 写法

```bash
PY="C:/Users/Administrator/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"
cd "D:/Codex/Some_project/Do_Modle_py"
"$PY" scripts/verify.py
"$PY" scripts/verify.py --skip-network
```

> `py.cmd` 是纯 ASCII 的三行包装脚本（内容只有一行 `"...python.exe" %*`），
> 不含中文，无编码风险。若它无法执行，用上面的「方式 B」直连 Python 即可。

---

## 三、实机验证结果（这是本轮最重要的部分）

### 3.1 全部核心字段核对通过

| 核对项 | 结果 |
|---|---|
| TCP `public-api.baostock.com:10030` | ✅ 可连通 |
| `get_instrument` | ✅ `sh.601872 招商轮船 board=MAIN list_date=2006-12-01` |
| `get_calendar` | ✅ 38 个交易日（2024-01~03 区间） |
| `get_bars` 不复权 | ✅ 38 根。首根 `2024-01-02 O=5.92 H=6.08 L=5.92 C=6.05 prev_close=5.88` |
| **`preclose` 映射** | ✅ `prev_close=5.88 > 0`（涨跌停基准） |
| **`volume` 单位** | ✅ **股**（5746 万股 ≈ 57 万手，成交额 ≈ 3.5 亿，合理） |
| **`adjustflag` 生效** | ✅ hfq `close=9.7510512` vs none `close=6.05`（不同 → 复权生效） |
| **分红字段 4/4 命中** | ✅ `dividOperateDate` / `dividStocksPs` / `dividReserveToStockPs` / `dividCashPsBeforeTax` |
| `get_dividends` | ✅ **13 条真实分红**（2016-06-22 起，现金 0.066 ~ 0.1 元/股） |

**`_FIELD_CANDIDATES` 全部命中**——分红字段的候选名策略有效。

### 3.2 真实数据特征（值得注意）

同步 2016-01-01 ~ 2026-09-01：

| 指标 | 值 |
|---|---|
| 交易日 | **2591** |
| 不复权 Bar | 2591 |
| 后复权 Bar | 2591 |
| 分红 | 13 条 |
| **停牌日** | **100 天（3.9%）** |
| `prev_close` 非法行 | **0** |
| 价格区间 | **3.30 ~ 21.91 元**（6.6 倍跨度） |
| 区间首根 | 2016-01-04 `close=6.41 prev_close=7.09`（**跌 9.59%，接近跌停**——2016 年 1 月熔断期） |

**三个直接结论**：

1. **停牌不是理论问题**：100 个停牌日真实存在，规则层的停牌处理是必需的
2. **不能用单一价格做资金分析**：股价 3.30 → 21.91 元，跨度 6.6 倍。同一笔 100 手订单在 2016 年是 3.3 万元，在 2026 年是 21.9 万元
3. **区间首日就接近跌停**：涨跌停建模不是"以防万一"，第一天就会用到

---

## 四、B&H 成本校验（真实数据，P4 的核心门槛提前达成）

```
标的: sh.601872 招商轮船
区间: 2024-01-02 ~ 2024-03-01   Bar 数: 38   分红: 0 条
信号日:   2024-01-02（首个有 Bar 的交易日）
成交日:   2024-01-03   成交价 6.08（开盘 6.08）
期末收盘: 2024-03-01  7.21

初始资金:           200,000.0000
期末净值(回测):     201,126.8992
期末净值(理想):     201,130.0000   ← 手工持有，零成本
差额:                     3.1008
  其中 滑点:              0.0000
  其中 费用:              3.1008
  预期差额合计:           3.1008
  **残差**:               0.0000
```

**[PASS] 残差 < 1e-6 → 回测净值与手工持有的差异完全由成本解释。规则层与撮合器可信。**

---

## 五、本轮发现的 3 个真实问题

### 问题 1（已修）：`query_stocks_in_risk` 服务端不支持

| 项 | 内容 |
|---|---|
| 现象 | `AttributeError` → 从子模块解析后 → **`10004020 错误的消息类型`** |
| 根因 | `query_stocks_in_risk` 定义在 `baostock.security.sectorinfo`，但**未在顶层导出**；且**服务端拒绝该消息类型**（已废弃） |
| 影响 | ST 名单批量查询不可用 |
| 缓解 | **单标的场景用 `Bar.is_st`**（日线自带 `isST` 列），完全不需要该接口；这同时避免了 2500 天 × 1 次查询 |
| 已改 | `get_risk_warning` 抛明确 `ProviderError` 并指向 `Bar.is_st` |

**附带修复**：`query_suspended_stocks` 同样未导出，但服务端**可用**。已加 `resolve_baostock_func()` 先试顶层、再试子模块。实测字段 `['updateDate', 'code', 'code_name']`。

### 问题 2（✅ **已解决：采用模型 B**）：滑点在低价股上被 tick 取整吃掉

**现象**：B&H 校验里 `滑点 = 0.0000`。

**根因**：成交价必须对齐 tick（0.01 元）。6.08 元 × 0.05% = **0.00304 元**，小于半个 tick（0.005），`round_to_tick` 后归零。

**已采用的方案 B**：`slippage > 0` 时，成交价**至少移动 1 个 tick**。

**实测效果**（1000 股，1 tick = 10 元）：

| 区间 | 开盘价 | 修正前成交价 | 修正后成交价 | 修正前滑点 | 修正后滑点 |
|---|---|---|---|---|---|
| 2016-06-02 | 4.88 | 4.88 | **4.89** | 0.0000 | 10.0000 |
| 2024-01-03 | 6.08 | 6.08 | **6.09** | 0.0000 | 10.0000 |

**⚠️ 需要知道的副作用：模型 B 在低价股上会放大滑点**

1 tick 占股价的比例随价格下降而上升：

| 股价 | 1 tick 占比 | 百分比模型(0.05%) | 模型 B 实际取值 |
|---|---|---|---|
| 3.30 元（2016 低点） | **0.303%** | 0.00165 元 | 0.01 元（**≈6 倍**） |
| 4.88 元 | 0.205% | 0.00244 元 | 0.01 元（≈4 倍） |
| 6.08 元 | 0.164% | 0.00304 元 | 0.01 元（≈3.3 倍） |
| 10.00 元 | 0.100% | 0.00500 元 | 0.01 元（≈2 倍） |
| **20.00 元** | **0.050%** | 0.01000 元 | 0.01 元（**恰好相等**） |
| 40.00 元 | 0.025% | 0.02000 元 | 0.02 元（百分比胜出） |

**交叉点 = 20 元**（`0.05% × P = 0.01`）。高于 20 元时百分比模型主导，低于 20 元时 1 tick 主导。

**这个"放大"其实更真实，不是纯粹的保守**：A股报价以 0.01 元为最小单位，
在 4.88 元的股票上，"0.05% 滑点" = 0.24 个 tick —— **物理上不可能实现**。
真实成交要么在原价、要么在 ±1 tick。所以百分比模型在低价股上本身就是错的，
模型 B 的 tick 量化才是正确形态。

**代价**：招商轮船 2016–2020 股价 3–10 元，滑点按 1 tick 计，成本被显著抬高。
若后续发现策略在低价区间被"过度惩罚"，可用 `CostConfig(slippage=0.0)` 关闭滑点做敏感性对照。

### 问题 3（**未修，P4 工作项**）：引擎尚未接入公司行为

真实数据有 **13 条分红**。当前 `BacktestEngine` 没有调用 `CorporateActionHandler`，所以：

- 跨分红日的回测**净值会偏低**（分红现金没进账户）
- 持仓数量在送转日**不会自动增加**

**已实现但未接线**：`rules/corporate_action.py` + `data.get_dividends()` 都已就绪，只差引擎里的一个钩子（约 20 行）。

**当前规避**：B&H 校验选了无分红区间（2024-01~03）。

---

## 六、P3 模块设计

```
do_modle/core/
├─ event.py      EventBus / EventType / Event      12 用例
├─ clock.py      Clock（回测/实盘统一时间源）       18 用例
├─ matching.py   MatchingEngine（唯一撮合点）       24 用例
├─ ledger.py     Ledger（唯一账务点）               14 用例
├─ recorder.py   Recorder（逐日快照）               12 用例
└─ backtest.py   BacktestEngine（编排）             21 用例
```

### 关键设计

| 设计点 | 说明 |
|---|---|
| **执行时序** | 信号在 T 日收盘产生 → 订单在 **T+1 开盘**成交。日单有效期 1 个交易日，次日停牌即拒单，不跨日顺延 |
| **未来函数守卫** | `match(signal_date=...)`：若成交 Bar 日期 ≤ 信号日 → 直接抛 `ValueError`。比"靠约定"可靠 |
| **成交价写死** | `MatchingEngine(execution_price="next_open")` 之外的值一律构造期报错 |
| **先卖后买** | A股 T 日卖出资金 T 日即可用于买入，先卖后买才能正确建模调仓 |
| **撮合无副作用** | `match` 不修改持仓簿；账务只在 `Ledger` 更新 |
| **风控注入** | `pre_trade_hook` 可调用对象，P6 的 `RiskGate` 从这里接入；**被拒订单进入结果**，风控不是装饰 |
| **确定性** | 无随机、无时间依赖；同输入两次运行净值**逐点相等**（`==`，不是 approx） |

### 两个核心门槛的测试

| 门槛 | 用例 |
|---|---|
| 同输入两次运行净值逐点相等 | `test_same_input_twice_gives_identical_nav` |
| B&H 差异 = 滑点 + 费用 | `test_buy_and_hold_gap_is_explained_by_cost` |

---

## 七、目录结构（累计）

```
Do_Modle_py/
├─ config/{base,stock_601872}.yaml
├─ do_modle/
│  ├─ objects.py / numeric.py / config.py / logging_setup.py   [P0]
│  ├─ rules/  cost · position · tradability · corporate_action · portfolio · account  [P1]
│  ├─ data/   provider · pit · cache · adapters/{baostock_src, local_cache}   [P2]
│  └─ core/   event · clock · matching · ledger · recorder · backtest        [P3]
├─ scripts/
│  ├─ verify.py            ★ 一键验证（环境+单测+连通性+字段核对）
│  ├─ sync_data.py         ★ 数据同步落库
│  ├─ validate_bh.py       ★ 真实数据 B&H 成本校验
│  └─ probe_baostock.py      早期探针（已被 verify.py 覆盖）
├─ data_store/market.sqlite   ★ 2591 交易日真实数据
└─ tests/  20 个文件 · 410 用例
```

---

## 八、下一步

| 优先级 | 工作项 | 阻塞关系 |
|---|---|---|
| **P3.5** | 引擎接入 `CorporateActionHandler`（13 条分红） | 跨分红区间回测的前置 |
| **决策** | 滑点模型选 A/B/C（建议 B） | 影响所有回测结论 |
| P4 | `evaluation/`：quantstats 报告 + 10 项陷阱清单 + akquant 对账（含 P4-0 探针） | 依赖 P3.5 |
| P5 | `strategy/` 因子库 + 航运板块截面 IC（N≥10） | 依赖 P4 |
| P6 | `execution/` RiskGate（9 条规则） | 依赖 P3 |
