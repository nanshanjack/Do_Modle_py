# P6-d · 正交源探针 + ⚠️ 一个推翻此前结论的方法论修正

> 状态：**探针受阻（数据源限流）；但过程中发现并修正了一个严重的方法论缺陷**
> 单测：**872 passed**（P6-d 新增 9 个自相关修正测试）

---

## 一、探针结果：两个数据源都不可用

### A. baostock 财务质量接口 —— **字段完美，但服务被限流**

**字段探针成功**（这是重要发现）：

```
query_profit_data  fields: ['code', 'pubDate', 'statDate', 'roeAvg', 'npMargin',
                            'gpMargin', 'netProfit', 'epsTTM', 'MBRevenue',
                            'totalShare', 'liqaShare']
query_growth_data  fields: ['code', 'pubDate', 'statDate', 'YOYEquity', 'YOYAsset',
                            'YOYNI', 'YOYEPSBasic', 'YOYPNI']
```

**`pubDate`（公告日）存在** —— PIT 完全可行：

| 字段 | 样例值 |
|---|---|
| `pubDate` | **2020-08-28** ← 公告日 |
| `statDate` | **2020-06-30** ← 报告期 |

**中报 6/30 报告期，8/28 才公告** —— 与 §2.5 的 `KIND_FINANCIAL_REPORT` 规则（报告期 + 法定滞后）一致。

**6 个接口可用**：`profit` / `operation` / `growth` / `balance` / `cashflow` / `dupont`

**但随后服务被限流**：

| 测试 | 结果 |
|---|---|
| `query_profit_data(2020, Q2)` 首次 | ✅ 0.04s 返回 |
| 连续调用约 700 次后 | ❌ 全部挂死（含 2016–2022 各年） |
| 等待 60s 后重试 | ❌ **连最简单的日线查询也挂死** |

**→ 不是接口问题，是 baostock 会话级限流/封禁。**

**影响评估**：

| 项 | 状态 |
|---|---|
| 已落库数据（22,703 K 线 + 估值 + 194 预告） | ✅ **完好，不受影响** |
| 新增财务数据（6 接口 × 41 季 × 12 只 = **2952 次调用**） | ❌ **当前不可行** |

### B. futu-mcp —— **整体不可用**

| 测试 | 结果 |
|---|---|
| `quote_research_rating_summary(SH.601872)` | ❌ internal error |
| `quote_shareholders_holding_changes(SH.601872)` | ❌ internal error |
| `quote_shareholders_holding_changes(HK.00700)` | ❌ internal error |
| `quote_stock_quote(["HK.00700"])` | ❌ internal error |

**连最基本的报价都 internal error** → Futu OpenD 网关未启动，或账号无行情权限。
**futu-mcp 不是可行的替代源。**

---

## 二、⚠️ 但探针过程中发现了一个**推翻此前结论**的方法论缺陷

### 问题：IC 序列高度自相关，朴素 t 值系统性高估约 2 倍

**起因**：在准备财务因子方案时意识到 —— 财务数据是**季度**的，因子值 3 个月不变，
IC 序列必然强自相关，标准 t 检验会严重高估显著性。

**于是回头检查已有因子**，结果发现**所有因子**都有这个问题：

| 因子 | 期数 | IC_IR | 朴素 t | **lag-1 自相关** | 有效 N | **修正 t** |
|---|---|---|---|---|---|---|
| turnover_mean_20 | 2517 | +0.1325 | **+6.65** | **0.694** | 620 | **+3.30** |
| intraday_range_20 | 2517 | +0.1158 | +5.81 | 0.685 | 603 | **+2.84** |
| vol_60 | 2476 | +0.1012 | +5.03 | 0.684 | 621 | **+2.52** |
| fc_score | 1701 | −0.0990 | −4.08 | 0.638 | 443 | **−2.08** |
| mom_60 | 2476 | −0.0822 | −4.09 | 0.674 | 595 | **−2.01** |

### 根因：**前向收益窗口重叠**

5 日前向收益下，相邻两日的 IC 有 **4/5 的收益区间重叠** → 自相关理论上限 ≈ 0.8。

**实测 lag-1 ≈ 0.68，与理论吻合。**

### 修正方法

```
T_eff = T / (1 + 2·Σρ_k)        # Bartlett 截断：累加正自相关，首次转负即停
t_correct = IC_IR × √T_eff
```

**实测膨胀倍数稳定在 2.00~2.05** —— 与理论 `√(1+2×0.8) = 2.0` 完全吻合。

---

## 三、修正后的搜索结果：**① 从 25/63 → 0/63**

| 闸 | 修正前（朴素 t） | **修正后（稳健 t）** |
|---|---|---|
| ① t 过 Bonferroni (z=3.354) | 25/63 | **0/63** |
| ② Walk-Forward 稳定 | 0/63 | 0/63 |
| ③ 全期 \|IC_IR\| ≥ 0.3 | 0/63 | 0/63 |

### 关键因子的修正前后

| 因子 | IC_IR | 朴素 t | **修正 t** | 膨胀 |
|---|---|---|---|---|
| turnover_mean_20 | +0.1325 | +6.65 | **+3.30** | 2.02× |
| sp_ttm | +0.1217 | +6.13 | +2.99 | 2.05× |
| intraday_range_20 | +0.1158 | +5.81 | +2.84 | 2.04× |
| max_ret_60 | +0.1086 | +5.40 | +2.70 | 2.00× |
| bp_mrq | +0.1037 | +5.22 | +2.57 | 2.03× |
| vol_60 | +0.1012 | +5.03 | +2.52 | 2.00× |
| mom_60 | −0.0822 | −4.09 | −2.01 | 2.04× |
| ma_gap_120 | −0.0804 | −3.96 | −1.95 | 2.02× |

> **`turnover_mean_20` 的修正 t = 3.30，极接近阈值 3.354** ——
> 说明信号**可能存在**，只是**统计上无法确认**。这比"完全没信号"更精确。

---

## 四、这个修正对之前结论的影响

| 结论 | 修正后状态 |
|---|---|
| 「25/63 因子全期统计显著」 | ❌ **推翻** → **0/63** |
| 「`fc_score` 利好出尽，t=−4.08 显著」 | ⚠️ **削弱** → t=−2.08，仅边缘 |
| 「估值因子提供增量（24/58 过 Bonferroni）」 | ❌ **推翻** → 修正后无一通过 |
| 「合成增益被相关性锁死 1.33×」 | ✅ **不受影响**（IC_IR 本身没错，只是 t 值错） |
| 「预告因子正交（0.044 vs 0.455）」 | ✅ **不受影响**（相关性计算与 t 无关） |
| 「三道闸后 0 存活」 | ✅ **结论不变**（甚至更稳固） |

**核心认识**：

> **IC_IR 的计算是正确的，错的只是 t 值。** 修正后结论更强：
> 不仅没有因子通过三道闸，**连"全期统计显著"这一层都没有因子能达到**。

---

## 五、实现

`ICSeries` 新增三个属性：

| 属性 | 含义 |
|---|---|
| `t_stat_naive` | 朴素 t（保留作对照，**不应作为判据**） |
| `effective_sample_size` | `T / (1 + 2Σρ_k)`，Bartlett 截断 |
| `t_stat` | **修正 t（正式判据）** |
| `t_inflation` | 膨胀倍数，>1.5 说明自相关严重 |

`t_stat` 语义已改为修正版，所有下游（`factor_search` / `factor_combo` / `xsec_ic`）自动生效。

**专项测试 9 个**：
- 强自相关序列的有效样本数 < T/5
- 白噪声的有效样本数 > 0.7T
- 修正 t 必须小于朴素 t
- 短序列（T<10）回退不修正
- 膨胀倍数：强自相关 >1.5，白噪声 <1.5

---

## 六、下一步

### 阻塞项

| 项 | 状态 | 应对 |
|---|---|---|
| baostock 限流 | ❌ 当前不可用 | **等待恢复**；恢复后用**限流友好**的抓取脚本（sleep + 重连 + 断点续传） |
| futu-mcp | ❌ 整体不可用 | 需用户检查 OpenD 网关 |

### 可立即做（不依赖新数据）

| 优先级 | 任务 |
|---|---|
| **P6-d-2** | **写限流友好的财务数据抓取脚本**（可后台长跑，带 `sleep` + 重连 + 断点续传）—— 等 baostock 恢复即可跑 |
| **P6-a** | `execution/` RiskGate（9 条规则）+ `OrderManager` 状态机 |
| **P7** | Walk-Forward + Monte Carlo（含执行延迟衰减） |

### 对财务因子的**预先警告**

即使数据到位，财务因子有**比现有因子更严重的自相关问题**：

* 季度数据 → 因子值 3 个月不变 → lag-1 自相关可能 **> 0.95**
* 有效独立样本数 ≈ 期数 / 60
* 12 只 × 41 季 = **492 个观测**，但有效独立样本可能只有 **数十个**

**所以财务因子必须用"按报告期采样"算 IC，而不是按日采样**，否则 t 值会虚高 10 倍以上。

**这个陷阱必须在建管线之前就写进设计**，否则拿到数据后会得到一堆假阳性。

---

## 七、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | 探针脚本 2952 次调用超时 | 缩到 3 只标的抽样 |
| 2 | 输出被缓冲，SIGTERM 后全部丢失 → 误判为"无输出" | 加 `-u` 无缓冲 |
| 3 | 误以为是"年份边界"问题 | 实测发现是**会话级限流**（连简单查询也挂） |
| 4 | futu-mcp 的 `rating_dimension_type` schema 自相矛盾（enum 字符串 / type 整数） | 用默认值绕过 |
| 5 | **朴素 t 值系统性高估 2 倍** | **加自相关稳健修正（本轮最重要修正）** |
