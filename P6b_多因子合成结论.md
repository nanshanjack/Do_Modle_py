# P6b · 多因子合成结论

> 状态：**合成框架完成，split-half 样本外评估完成**
> 结论：**合成方向正确，但增益被因子相关性数学锁死在 1.33×，无法跨过可用门槛**
> 单测：**836 passed**

---

## 一、为什么要 split-half（**方法纪律**）

若用**全期** IC 挑因子再在全期评估合成结果，得到的是**过拟合估计** ——
挑出来的因子本就在全期表现最好，合成当然好看。

**正确做法**：在 A 段选因子与定符号 → 在 B 段评估，反向再做一次。

```
前半→后半：训练 2016-01-01~2021-12-31  →  测试 2021-12-31~2026-09-01
后半→前半：训练 2021-12-31~2026-09-01  →  测试 2016-01-01~2021-12-31
```

**符号对齐**：`sign = +1 if IC_IR_训练 > 0 else −1`，由训练段决定，**不能事后按测试段调**。

**等权**：不做 IC 加权（那会引入更多拟合参数）。

---

## 二、基线：单因子全期 |IC_IR| 前 8

| 因子 | 类别 | 全期 \|IC_IR\| |
|---|---|---|
| turnover_mean_20 | valuation | **0.1325** |
| sp_ttm | valuation | 0.1217 |
| intraday_range_20 | volatility | 0.1158 |
| turnover_mean_60 | valuation | 0.1136 |
| max_ret_60 | lottery | 0.1086 |
| bp_mrq | valuation | 0.1037 |
| vol_40 | volatility | 0.1020 |
| vol_60 | volatility | 0.1012 |

**单因子 |IC_IR| 最大值 = 0.1325**（可用门槛 0.3）

---

## 三、split-half 样本外结果（**唯一无偏**）

### 前半 → 后半

| 选中因子 | 符号 | 训练 IC_IR | 测试 IC_IR |
|---|---|---|---|
| intraday_range_20 | + | +0.1215 | +0.1082 |
| turnover_mean_20 | + | +0.1180 | **+0.1559** |
| sp_ttm | + | +0.1134 | +0.1373 |
| mom_20 | − | −0.0946 | −0.0478 |
| rev_20 | + | +0.0946 | +0.0478 |

**合成后测试段：IC_IR = +0.1546，t = +5.19，期数 1126，平均 N = 11.3**

### 后半 → 前半

| 选中因子 | 符号 | 训练 IC_IR | 测试 IC_IR |
|---|---|---|---|
| bp_mrq | + | +0.1764 | +0.0611 |
| vol_40 | + | +0.1710 | +0.0573 |
| cfp_ttm | − | −0.1588 | −0.0543 |
| turnover_mean_60 | + | +0.1568 | +0.0867 |
| turnover_mean_20 | + | +0.1559 | +0.1180 |

**合成后测试段：IC_IR = +0.1072，t = +4.00，期数 1392，平均 N = 6.7**

### 汇总

| 项 | 值 |
|---|---|
| 单因子最好 \|IC_IR\| | 0.1325 |
| 合成 OOS（前半→后半） | **+0.1546** |
| 合成 OOS（后半→前半） | **+0.1072** |
| 可用门槛 | **0.30** |
| **判定** | **未达标** |

**合成有提升（0.1325 → 0.155，+17%），但幅度不足。**

**注意符号稳定性**：两段的 5 个选中因子里，`turnover_mean_20` 都入选且都是正号
（训练 +0.118 / +0.156，测试 +0.156 / +0.118）—— 这是全库最稳的因子。

---

## 四、根因：因子相关性锁死了合成上限

### 相关矩阵（逐期横截面秩相关的均值）

| | turnover_20 | sp_ttm | intraday_20 | turnover_60 | max_ret_60 | bp_mrq | vol_40 | vol_60 |
|---|---|---|---|---|---|---|---|---|
| **turnover_20** | 1.00 | 0.41 | 0.48 | **0.92** | 0.42 | 0.32 | 0.49 | 0.49 |
| **sp_ttm** | 0.41 | 1.00 | 0.19 | 0.40 | 0.23 | **0.57** | 0.16 | 0.18 |
| **intraday_20** | 0.48 | 0.19 | 1.00 | 0.39 | 0.55 | 0.30 | **0.79** | **0.74** |
| **turnover_60** | 0.92 | 0.40 | 0.39 | 1.00 | 0.41 | 0.31 | 0.43 | 0.49 |
| **max_ret_60** | 0.42 | 0.23 | 0.55 | 0.41 | 1.00 | 0.28 | 0.67 | 0.72 |
| **bp_mrq** | 0.32 | 0.57 | 0.30 | 0.31 | 0.28 | 1.00 | 0.27 | 0.28 |
| **vol_40** | 0.49 | 0.16 | 0.79 | 0.43 | 0.67 | 0.27 | 1.00 | **0.86** |
| **vol_60** | 0.49 | 0.18 | 0.74 | 0.49 | 0.72 | 0.28 | 0.86 | 1.00 |

**平均 |相关| = 0.455**

**最相关的几对**：
- `turnover_mean_20` ↔ `turnover_mean_60` = **0.92**（几乎是同一个因子）
- `vol_40` ↔ `vol_60` = **0.86**
- `intraday_range_20` ↔ `vol_40` = 0.79
- `vol_60` ↔ `max_ret_60` = 0.72

**最不相关的**：`sp_ttm` ↔ `vol_40` = 0.16、`sp_ttm` ↔ `vol_60` = 0.18
—— 营收收益率与波动率是最有互补性的组合。

### 理论增益上限

等权 K 因子合成的 IC_IR 提升：

```
增益 = √( K / (1 + (K−1)·ρ̄) )
```

K=5，ρ̄=0.455 → **增益 = 1.33×**

**验证**：单因子最好 0.1325 × 1.33 = **0.176**。
实测 OOS 0.155 / 0.107 —— **与理论同量级，吻合。**

> **这说明实测结果不是运气不好，而是被相关性数学锁定的必然结果。**

---

## 五、定论：三条路径的定量要求

| 路径 | 定量要求 | 可行性 |
|---|---|---|
| **单因子更强** | 单因子 IC_IR ≥ **0.226**（现 0.133） | 需新信号源 |
| **因子不相关** | ρ̄ = 0 → 增益 √5 = **2.24×** → 只需单因子 0.134 | ❌ **航运板块不可能**（同行业同周期） |
| **扩大截面** | N 从 8.7 → 100，IC 估计噪声 0.36 → 0.10 | ⚠️ 偏离单标的交易目标 |

**唯一现实路径：换更强信号源。**

> **合成是正确的方法论，但量级被相关性锁死。**
> 继续在现有 58 个因子上做组合优化，数学上不可能跨过 0.3 门槛。

---

## 六、交付

| 模块 | 内容 |
|---|---|
| `do_modle/strategy/composite.py` | 横截面 z-score / 排名 + 等权合成 + 因子相关性（21 用例） |
| `do_modle/numeric.py` | **下沉** `rank` / `spearman` / `pearson` / `mean` / `std`（供两层共用） |
| `scripts/factor_combo.py` | split-half 样本外评估 + 相关矩阵 + 理论增益 |

**架构修正**：`composite.py` 原直接 import `evaluation.xsec`，**违反红线**
（`strategy` 不得 import `evaluation`）。已把秩统计下沉到 `numeric.py`，
`xsec.py` 与 `composite.py` 均从 `numeric` 取用。

---

## 七、下一步

| 优先级 | 任务 | 说明 |
|---|---|---|
| **P6-c** | **运价数据可得性探针** | 唯一可能带来质变的方向。**只探针，不建管线** |
| P6-a | `execution/` RiskGate（9 条规则）+ `OrderManager` | 链路完整性所需 |
| P7 | Walk-Forward + Monte Carlo（含执行延迟衰减） | 鲁棒性验证 |

### P6-c 探针的具体目标

| 数据 | 用途 | 可得性 |
|---|---|---|
| BDI / BDTI / BCTI | 运价水平 | ⚠️ 项目历史结论已六次验证 **BDTI 无预测力** |
| CCFI / SCFI | 集运运价 | 需探针 |
| 分航线运价（VLCC / 成品油 / 干散） | **更细分**，可能比综合指数有效 | 需探针 |
| 运力交付 / 拆解量 | 供给侧 | 需探针 |
| 股价与运价的**背离** | 背离可能比水平更有信息 | 可用现有数据构造 |

> ⚠️ **纪律提醒**：BDTI 已被六次验证无效。若要用运价，
> 必须是**更细分的航线运价**或**运价与股价的背离**，
> 且必须走**同一套三重闸** —— 不能因为"这次不一样"就放低标准。

---

## 八、踩过的坑

| # | 问题 | 修正 |
|---|---|---|
| 1 | `composite.py` 直接 import `evaluation.xsec` → **违反架构红线** | 秩统计下沉到 `numeric.py`，两层共用 |
| 2 | `panel_correlation` 里留了一段无用的死循环（算了但没用到） | 清理 |
| 3 | 测试断言 `combo["b"] != 0`，但 b 恰是中位数 → z-score = 0 | 改用 4 个标的，避免中位数巧合 |
