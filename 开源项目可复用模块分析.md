# GitHub 开源项目可复用模块分析报告

> 目标：从开源项目中识别**可借鉴并二次开发的模块与组件**，规划自有交易系统
> 调研日期：2026-09-17
> 本报告为工程分析，**不构成投资建议**

---

## 一、调研项目总览

| 项目 | Stars | 定位 | 语言 | **许可证** |
|---|---|---|---|---|
| **[vnpy/vnpy](https://github.com/vnpy/vnpy)** | 45,412 | **实盘交易框架**（CTP/股票/期权/数字货币） | Python | **MIT** ✅ |
| **[microsoft/qlib](https://github.com/microsoft/qlib)** | 48,619 | AI 量化研究平台 | Python | **MIT** ✅ |
| **[ricequant/rqalpha](https://github.com/ricequant/rqalpha)** | 6,771 | **A股回测 + 交易框架** | Python | ⚠️ **非商业 Apache 2.0 / 商业需授权** |
| [ranaroussi/quantstats](https://github.com/ranaroussi/quantstats) | 7,641 | 组合绩效分析 | Python | Apache 2.0 ✅ |
| [marketcalls/vectorbt-backtesting-skills](https://github.com/marketcalls/vectorbt-backtesting-skills) | 203 | **回测方法论知识库** | Markdown | MIT ✅ |
| [akfamily/akquant](https://github.com/akfamily/akquant) | 2,325 | A股量化回测框架（Rust+Python） | Python | MIT ✅ |

### ⚠️ 许可证提醒（重要）

**`rqalpha` 的许可证是分层的**：
```
非商业用途（个人/高校/研究所）→ Apache License 2.0，可自由使用
商业用途（任何商业目的）      → 需米筐科技书面授权，否则追究侵权
```
**若你的项目有商业意图，rqalpha 的代码只能"参考思路"，不能直接复制。**
（本项目其余模块的替代：vnpy MIT 可自由使用）

---

## 二、模块级可复用清单（核心）

### 2.1 ★★★ 最高价值：A股规则层（我完全缺失）

**来源：`ricequant/rqalpha`**

| 文件 | 类/函数 | 提供什么 | 我缺什么 |
|---|---|---|---|
| `rqalpha/portfolio/position.py` | **`PositionQueue`** | **T+1 的 FIFO 队列实现** | ✅ **完全没有** |
| `rqalpha/portfolio/position.py` | `Position.today_closable` | 当日可卖数量（= 总量 − 昨仓） | ✅ 完全没有 |
| `rqalpha/portfolio/position.py` | `Position.closable` | 可平仓位（扣除挂单） | ✅ 完全没有 |
| `rqalpha/portfolio/position.py` | `Position.avg_price` | 加权平均成本 | ⚠️ 部分 |
| `rqalpha/portfolio/position.py` | `trading_pnl` / `position_pnl` / `daily_pnl` | **区分"当日成交盈亏"与"昨仓盈亏"** | ✅ 完全没有 |
| `rqalpha/portfolio/position.py` | `handle_split` | 送股/拆股处理 | ✅ 完全没有 |
| `rqalpha/portfolio/capital_gains_tax.py` | **`CapitalGainsTaxMixin`** | **金融商品转让增值税 + 附加税 + 亏损跨月抵扣** | ✅ **完全没有** |
| `rqalpha/portfolio/account.py` | `Account` | 账户（资金/持仓/盈亏） | ✅ 完全没有 |

#### 核心设计：`PositionQueue` 如何实现 T+1

```python
class PositionQueue:
    def __init__(self):
        self._queue: Deque[Tuple[date, int]] = deque()   # [(开仓日期, 数量), ...]

    def handle_trade(self, delta_quantity, trading_date, close_today=False):
        # 平仓：从队首（最早买入）开始消耗 → FIFO → 天然"先平昨仓"
        while delta_quantity and self._queue:
            d, qty = self._queue[0]
            if abs(qty) <= abs(delta_quantity):
                close_details.append((d, -qty))
                delta_quantity += qty
                self._queue.popleft()
            else:
                close_details.append((d, delta_quantity))
                self._queue[0] = (d, qty + delta_quantity)
                delta_quantity = 0
        # 开仓：追加到队尾，记录开仓日期
        if self._queue and self._queue[-1][0] == trading_date:
            _, qty = self._queue.pop()
            self._queue.append((trading_date, qty + delta_quantity))
        else:
            self._queue.append((trading_date, delta_quantity))
```

**配套机制**：
```python
def before_trading(self, trading_date):
    self._old_quantity = self._quantity        # 日初：昨日持仓结转
    ...

@property
def today_closable(self):
    return self._quantity - self._old_quantity   # T+1：当日买入不可卖
```

**为什么这个设计好**：
- 用**队列日期**表达"持仓批次"，天然支持 FIFO
- 不需要额外标记"哪些是今仓"，日期即状态
- 支持**部分平仓**（`close_details` 返回每批平掉的数量）

### 2.2 ★★ 高价值：回测引擎与扩展机制

**来源：`ricequant/rqalpha`**

| 目录/文件 | 提供什么 |
|---|---|
| `rqalpha/core/` | **事件驱动引擎**（`EVENT.BEFORE_TRADING` / `BAR` / `TICK` / `AFTER_TRADING`） |
| `rqalpha/mod/` | **Mod 扩展机制**（可插拔：`rqalpha-mod-vnpy` / `-ctp` / `-tushare`） |
| `rqalpha/interface.py` | 抽象接口定义（`AbstractPosition` / `AbstractStrategy` 等） |
| `rqalpha/model/` | 数据模型（`Instrument` / `Order` / `Trade`） |
| `rqalpha/apis/` | 策略 API |

**Mod 机制的价值**：它把"回测引擎"与"数据源/交易通道"解耦。
→ **这正是我需要的架构**：核心引擎不依赖具体数据源。

### 2.3 ★★ 高价值：实盘交易接口

**来源：`vnpy/vnpy`（MIT，可自由使用）**

| 模块 | 提供什么 |
|---|---|
| `vnpy/trader/` | **交易核心**（`MainEngine` / `EventEngine` / `BaseGateway`） |
| `vnpy/trader/gateway/` | 各券商/交易所网关（CTP / 中泰XTP / 华鑫奇点等） |
| `vnpy/trader/object.py` | 统一数据对象（`TickData` / `OrderData` / `TradeData` / `PositionData`） |
| `vnpy/trader/utility.py` | 工具（`BarGenerator` / `ArrayManager`） |
| `vnpy/alpha/` | 因子研究模块 |

**关键价值**：**实盘对接是自建成本最高的部分**，vnpy 已覆盖主流券商接口。

### 2.4 ★★ 高价值：因子与模型（研究层）

**来源：`microsoft/qlib`（MIT）**

| 模块 | 提供什么 |
|---|---|
| `qlib/contrib/data/handler.py` | **Alpha158 / Alpha360 因子定义** |
| `qlib/contrib/model/` | **23 个 SOTA 模型**（LightGBM/LSTM/Transformer/HIST/KRNN…） |
| `qlib/data/` | PIT 数据设计（`ExpressionCache` / `DatasetCache`） |
| `qlib/contrib/evaluate.py` | **IC / 分组收益 / 月度 IC** 评估 |
| `qlib/contrib/strategy/` | 策略（TopkDropout / EnhancedIndexing） |
| `qlib/workflow/` | 实验管理与模型滚动 |

**注**：qlib 的**数据格式是专有的**（`qlib_bin`），直接用它需要转换。
→ 建议**借鉴其因子定义与评估口径**，不强制使用其数据层。

### 2.5 ★ 中价值：绩效分析

**来源：`ranaroussi/quantstats`（Apache 2.0）**

| 模块 | 提供什么 |
|---|---|
| `quantstats/stats.py` | 30+ 指标（Sharpe / Sortino / Calmar / 回撤分析 / 胜率 / 盈亏比） |
| `quantstats/plots.py` | 可视化（回撤曲线 / 月度热力图 / 滚动指标） |
| `quantstats/reports.py` | HTML tearsheet |

→ **可 pip 安装直接使用**，无需二次开发。

### 2.6 ★ 中价值：回测方法论（纯文档）

**来源：`marketcalls/vectorbt-backtesting-skills`（MIT）**

| rule file | 提供什么 |
|---|---|
| `pitfalls.md` | **10 个回测陷阱 + 上线检查清单** |
| `indian-market-costs.md` | **成本模型结构**（可类比 A股：0.111%/边 + 20元/笔 + 0.05% 滑点） |
| `walk-forward.md` | WFE（Walk-Forward Efficiency）比率 |
| `robustness-testing.md` | **Monte Carlo**：交易打乱 / 噪声注入 / 参数敏感性 / 延迟测试 |
| `parameter-optimization.md` | 参数热力图（看"宽稳定区"而非"孤立尖峰"） |

---

## 三、借鉴与改造方式（三种模式）

| 模式 | 适用 | 具体做法 | 项目 |
|---|---|---|---|
| **① 直接依赖** | 无许可证风险 + 无需改造 | `pip install`，作为库调用 | quantstats、vnpy（MIT） |
| **② 移植改造** | 核心逻辑需要定制 | 读源码 → 重写核心类 → 适配自有数据格式 | **rqalpha 的 `PositionQueue` / `CapitalGainsTaxMixin`** |
| **③ 参考设计** | 许可证受限 或 改造成本高于收益 | 只借鉴接口与思路，不复制代码 | qlib（数据格式重）、rqalpha 商业用途 |

### 3.1 重点：A股规则层的移植方案

**`PositionQueue` 移植**（推荐**重写**，不复制代码）：

```python
# 自有实现（思路借鉴 rqalpha，代码自写）
class PositionLot:
    """持仓批次：记录每笔开仓的日期与数量"""
    __slots__ = ("open_date", "quantity", "cost_price")
    def __init__(self, open_date, quantity, cost_price):
        self.open_date, self.quantity, self.cost_price = open_date, quantity, cost_price

class PositionBook:
    """A股持仓簿：FIFO 队列实现 T+1"""
    def __init__(self):
        self._lots: Deque[PositionLot] = deque()

    @property
    def total(self) -> int:
        return sum(l.quantity for l in self._lots)

    @property
    def today_bought(self) -> int:
        """当日买入量 → T+1 不可卖"""
        today = self._current_date()
        return sum(l.quantity for l in self._lots if l.open_date == today)

    @property
    def sellable(self) -> int:
        """可卖量 = 总量 − 当日买入"""
        return self.total - self.today_bought

    def buy(self, date, qty, price):
        if self._lots and self._lots[-1].open_date == date:
            self._lots[-1].quantity += qty        # 同日加仓合并
        else:
            self._lots.append(PositionLot(date, qty, price))

    def sell(self, date, qty, price) -> list:
        """FIFO 卖出，返回平仓明细 [(开仓日, 数量, 成本价)]"""
        if qty > self.sellable:
            raise ValueError(f"T+1 限制：可卖 {self.sellable}，请求 {qty}")
        details = []
        while qty > 0:
            lot = self._lots[0]
            take = min(lot.quantity, qty)
            details.append((lot.open_date, take, lot.cost_price))
            lot.quantity -= take
            qty -= take
            if lot.quantity == 0:
                self._lots.popleft()
        return details
```

**为什么重写而非复制**：
1. **许可证**（rqalpha 商业用途受限）
2. **依赖解耦**（rqalpha 的 Position 依赖其 `Environment` 全局单例，移植需大量适配）
3. **简化**（我只需 A股股票，不需期货/期权/融资融券）

**成本模型**（A股，自写）：
```python
class AShareCostModel:
    """A股交易成本模型（参照 vectorbt-skills 的结构）"""
    COMMISSION_RATE = 0.00025      # 佣金 0.025%（双边，可谈）
    STAMP_TAX_RATE  = 0.0005       # 印花税 0.05%（仅卖出）
    TRANSFER_RATE   = 0.00001      # 过户费 0.001%（双边）
    MIN_COMMISSION  = 5.0          # 最低佣金 5 元
    SLIPPAGE        = 0.0005       # 滑点 0.05%（流动大盘股最低）

    def buy_cost(self, amount):
        comm = max(amount * self.COMMISSION_RATE, self.MIN_COMMISSION)
        return comm + amount * self.TRANSFER_RATE

    def sell_cost(self, amount):
        comm = max(amount * self.COMMISSION_RATE, self.MIN_COMMISSION)
        return comm + amount * (self.STAMP_TAX_RATE + self.TRANSFER_RATE)

    def round_trip(self, amount):
        """往返总成本率（用于快速估算）"""
        return (self.buy_cost(amount) + self.sell_cost(amount)) / amount
        # ≈ 0.111% + 滑点 0.1% ≈ 0.21%（不含滑点 0.111%）
```

---

## 四、二次开发方案（分阶段）

### Phase 0：依赖引入与验证（不写业务代码）
| 步骤 | 动作 | 验证 |
|---|---|---|
| 0.1 | `pip install quantstats vnpy` | 导入成功 |
| 0.2 | 用 quantstats 对现有回测结果生成 tearsheet | 能输出 30+ 指标 |
| 0.3 | 通读 rqalpha `position.py` / `capital_gains_tax.py` | 理解 T+1 与税务逻辑 |
| 0.4 | 通读 vectorbt-skills 的 `pitfalls.md` / `robustness-testing.md` | 形成检查清单 |

### Phase 1：自建 A股规则层（**最高优先级**）
| 步骤 | 动作 | 来源 |
|---|---|---|
| 1.1 | `PositionBook`（FIFO 队列 + T+1） | 思路借鉴 rqalpha，代码自写 |
| 1.2 | `AShareCostModel`（佣金/印花税/过户费/滑点） | 结构借鉴 vectorbt-skills |
| 1.3 | 涨跌停可交易性（涨停不可买、跌停不可卖） | 自写 |
| 1.4 | `Account`（资金/持仓/盈亏，区分今仓昨仓盈亏） | 思路借鉴 rqalpha |
| 1.5 | 单元测试（构造涨停/跌停/T+1 场景） | 沿用 fail-to-pass 文化 |

**Phase 1 产出**：一个**符合 A股真实约束**的回测执行层。

### Phase 2：回测引擎重构
| 步骤 | 动作 |
|---|---|
| 2.1 | 事件驱动引擎（借鉴 rqalpha 的 `EVENT` 设计） |
| 2.2 | 多标的支持（组合视角） |
| 2.3 | 接入 Phase 1 的规则层 |
| 2.4 | 用 quantstats 输出标准 tearsheet |

### Phase 3：因子与模型层
| 步骤 | 动作 | 来源 |
|---|---|---|
| 3.1 | 因子注册机制（名称/公式/时点/来源） | 借鉴 qlib 的 handler 组织 |
| 3.2 | 扩充因子库（现有 11 → 30+） | 参考 Alpha158 定义 |
| 3.3 | 接入 LightGBM（保留 OLS 作基准） | 参考 qlib `contrib/model` |
| 3.4 | IC / 分组收益 / 月度 IC 评估 | 借鉴 qlib `contrib/evaluate` |

### Phase 4：鲁棒性验证
| 步骤 | 动作 | 来源 |
|---|---|---|
| 4.1 | Walk-forward + WFE 比率 | vectorbt-skills `walk-forward.md` |
| 4.2 | **Monte Carlo**（交易打乱/噪声注入/参数敏感性/延迟） | vectorbt-skills `robustness-testing.md` |
| 4.3 | 参数热力图（验证宽稳定区） | 同上 |
| 4.4 | 按 `pitfalls.md` 的 10 项清单逐条自检 | 同上 |

### Phase 5：实盘对接（若策略通过）
| 步骤 | 动作 | 来源 |
|---|---|---|
| 5.1 | 接入 vnpy 的 `EventEngine` / `BaseGateway` | **vnpy（MIT，可直接用）** |
| 5.2 | 对接券商网关（CTP / XTP 等） | vnpy gateway |
| 5.3 | Paper trading ≥1 个月 | — |

---

## 五、独立开发的关键环节

### 5.1 三个必须自建的部分

| 环节 | 为什么必须自建 |
|---|---|
| **A股规则层** | rqalpha 商业受限；且我只需股票，不需期货/期权，移植反而更重 |
| **因子与信号逻辑** | 这是策略的核心资产，不应依赖外部 |
| **组合与风险决策** | 同上，且各人风险偏好不同 |

### 5.2 三个应当依赖的部分

| 环节 | 为什么依赖 |
|---|---|
| **实盘交易接口** | 自建成本极高（各券商协议不同）；vnpy 是 MIT，可自由用 |
| **绩效指标计算** | 标准化程度高，自建无收益；quantstats 直接可用 |
| **回测方法论** | 纯知识，直接采用标准即可 |

### 5.3 关键设计原则

1. **数据层与引擎解耦**（借鉴 rqalpha 的 Mod 机制）——避免换数据源就要改引擎
2. **规则层独立可测**（T+1/成本/涨跌停各自独立单元测试）
3. **归因 ≠ 预测**（本项目已验证的教训，架构上要区分"研究工具"与"交易信号"）
4. **每步可验证**（沿用 fail-to-pass 文化）

### 5.4 一个必须正视的前提

**开源项目能解决"工程完备性"，但不能解决"策略有效性"。**

本项目已六次验证：BDTI/油价无预测力、跳空吃掉收益、超额收益 t=−1.27、跑输买入持有。

**所以引入这些模块后，最可能的结论仍是"策略不可用"**——
但至少**结论会是确定的**，且**未来的新想法能被快速、严格地验证**。

---

## 六、总结：一张表看清"用什么、怎么用"

| 需求 | 用什么 | 方式 | 许可证 |
|---|---|---|---|
| **A股 T+1 / 持仓批次** | rqalpha `PositionQueue` | **参考思路，自写** | ⚠️ 商业受限 |
| **A股成本（佣金/印花税/过户费）** | vectorbt-skills 成本结构 | 自写 | MIT ✅ |
| **增值税/附加税（机构）** | rqalpha `CapitalGainsTaxMixin` | 参考思路，自写 | ⚠️ 商业受限 |
| **事件驱动引擎** | rqalpha `core/` 的 EVENT 设计 | 参考设计 | ⚠️ 商业受限 |
| **Mod 扩展机制** | rqalpha `mod/` | 参考设计 | ⚠️ 商业受限 |
| **实盘交易接口** | **vnpy** | **直接依赖** | **MIT ✅** |
| **因子定义** | qlib Alpha158 | 参考定义，自写 | MIT ✅ |
| **SOTA 模型** | qlib `contrib/model/` | 参考实现 | MIT ✅ |
| **IC / 分组收益评估** | qlib `contrib/evaluate` | 参考口径 | MIT ✅ |
| **绩效指标** | **quantstats** | **直接依赖** | Apache 2.0 ✅ |
| **回测陷阱清单** | vectorbt-skills `pitfalls.md` | **直接采用** | MIT ✅ |
| **Monte Carlo 鲁棒性** | vectorbt-skills `robustness-testing.md` | **直接采用** | MIT ✅ |

**最高优先级的两件事**（与之前结论一致）：
1. **自建 A股规则层**（T+1 + 成本 + 涨跌停）——这是回测可信度的地基
2. **采用 vectorbt-skills 的鲁棒性检验标准**——这是判断策略可用性的标尺

**理由**：在"策略是否有效"未确定前，引入更多因子/模型都是**在不确定的地基上盖楼**。
