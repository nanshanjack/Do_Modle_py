# P0 骨架 + P1 规则层 · 实现说明

> 对应设计文档：`A股量化交易系统_实施方案设计.md` §4.1、§6（P0/P1）
> 状态：**已实现并通过全部单测**（171 passed）
> 佣金前提：**万五（0.0005）+ 免五（`min_commission = 0.0`）**

---

## 一、交付概览

| 项 | 结果 |
|---|---|
| 测试总数 | **171 passed** |
| `rules/` 层测试 | **126**（远超要求的 30 个） |
| 运行耗时 | 0.63s |
| 独立运行 | 每个测试文件可单独运行；任意 cwd 下可运行（已验证） |
| 架构守卫 | 已验证**会真实失败**（合成越层依赖后被拦下 2 个用例） |
| 外部依赖 | `rules/` 层**零第三方依赖**（由 `test_rules_layer_has_zero_third_party_deps` 强制） |

各文件用例数：

| 文件 | 用例数 | 归属 |
|---|---|---|
| `test_rules_tradability.py` | 37 | P1 |
| `test_rules_position.py` | 27 | P1 |
| `test_rules_cost.py` | 21 | P1 |
| `test_rules_account.py` | 19 | P1 |
| `test_rules_portfolio.py` | 13 | P1 |
| `test_rules_corporate_action.py` | 9 | P1 |
| `test_architecture.py` | 7 | P0 |
| `test_config.py` | 15 | P0 |
| `test_numeric.py` | 23 | P0 |

---

## 二、目录结构（实际）

```
Do_Modle_py/
├─ pyproject.toml              # 打包 + pytest 配置（testpaths=tests）
├─ conftest.py                 # ★ 把项目根插入 sys.path，保证任意 cwd 可运行
├─ config/
│  ├─ base.yaml                # 全局：成本(万五免五)/撮合/PIT/风控/回测
│  └─ stock_601872.yaml        # 标的覆盖
├─ do_modle/
│  ├─ __init__.py              # 版本 + 分层说明
│  ├─ objects.py          [P0] # ★ 全系统共享数据契约
│  ├─ numeric.py          [P0] # 价格取整 / 手数对齐（Decimal HALF_UP）
│  ├─ config.py           [P0] # YAML → 冻结 dataclass
│  ├─ logging_setup.py    [P0] # 日志
│  └─ rules/              [P1] # ★ A股规则层，零外部依赖
│     ├─ __init__.py
│     ├─ cost.py               # AShareCostModel
│     ├─ position.py           # PositionBook（FIFO + T+1）
│     ├─ tradability.py        # LimitRuleTable + TradabilityGate
│     ├─ corporate_action.py   # CorporateActionHandler
│     ├─ portfolio.py          # Portfolio（多标的容器）
│     └─ account.py            # Account
└─ tests/
   ├─ __init__.py              # 使测试可用绝对导入
   ├─ _helpers.py              # 常量与构造器（D0/D1/D2、make_bar、make_instrument）
   ├─ conftest.py              # pytest 夹具（薄封装 _helpers）
   ├─ test_architecture.py [P0]
   ├─ test_numeric.py      [P0]
   ├─ test_config.py       [P0]
   └─ test_rules_*.py      [P1] × 6
```

---

## 三、依赖关系

### 3.1 实际依赖图（已实现部分）

```
objects.py ──┬──> rules/*.py
numeric.py ──┘
                  ↑
config.py ────────┘（仅测试里串联，config 自身零内部依赖）
logging_setup.py（零内部依赖）
```

**方向单一，无环。** `rules` 只依赖 `objects` 与 `numeric`。

### 3.2 架构守卫（`test_architecture.py`，7 个用例）

| 用例 | 断言内容 |
|---|---|
| `test_package_tree_is_not_empty` | 包树存在且含 `rules.cost` / `rules.position` |
| `test_no_upward_or_forbidden_imports` | **核心**：任何模块只能依赖白名单内的内部模块 |
| `test_rules_layer_does_not_import_data` | 禁令 1：`rules` 不得 import `data` |
| `test_rules_layer_has_zero_third_party_deps` | `rules` 只允许标准库 + `do_modle` |
| `test_strategy_does_not_import_evaluation` | 禁令 3：归因 ≠ 预测（当前为空，守卫未来） |
| `test_only_app_may_import_app` | 禁令 4：应用层是叶子 |
| `test_all_shared_objects_defined_in_objects_module` | `rules` 不得重复定义 `Bar`/`Trade`/`Instrument` 等 |

**有效性已验证**：在临时副本中新建 `do_modle/rules/violation.py` 引入 `do_modle.data.provider`，`test_no_upward_or_forbidden_imports` 与 `test_rules_layer_does_not_import_data` **双双失败**并给出准确路径。守卫不是摆设。

---

## 四、`rules/` 规则清单 → 测试用例覆盖矩阵

### 4.1 `cost.py` — `AShareCostModel`（21 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | 默认费率 = 万五 + 免五 | `test_default_config_matches_user_rates` |
| 2 | **免五**：小额订单按比例收佣金，不触底 5 元 | `test_no_minimum_commission_on_small_order` |
| 3 | 最低佣金仍可配置（兼容其它券商口径） | `test_minimum_commission_still_configurable` |
| 4 | 滑点方向：买入上滑、卖出下滑 | `test_slippage_direction_buy_up_sell_down` |
| 5 | 滑点成本单独记录 | `test_slippage_cost_is_recorded` |
| 6 | 买入费用四项分解 | `test_buy_fee_breakdown` |
| 7 | 买入无印花税 | `test_buy_has_no_stamp_tax` |
| 8 | 卖出费用五项分解（含印花税） | `test_sell_fee_breakdown` |
| 9 | 卖出现金流为正 | `test_sell_cash_flow_is_positive` |
| 10 | 卖出费用 > 买入费用（多一道印花税） | `test_sell_cost_exceeds_buy_cost_for_same_amount` |
| 11 | 往返成本率（不含滑点）= **0.152%** | `test_round_trip_rate_excluding_slippage` |
| 12 | 往返成本率（含滑点）= **0.252%** | `test_round_trip_rate_including_slippage` |
| 13 | **免五后无单笔金额临界点**（大小单同率） | `test_round_trip_rate_is_amount_independent_when_no_min_commission` |
| 14 | 零金额边界 | `test_round_trip_rate_zero_amount` |
| 15 | 数量必须为正 | `test_buy_rejects_non_positive_quantity`（参数化 ×2） |
| 16 | 价格必须为正 | `test_sell_rejects_non_positive_price`（参数化 ×2） |
| 17 | 负费率拒绝 | `test_negative_rate_rejected` |
| 18 | `from_mapping` 忽略未知键 | `test_from_mapping_ignores_unknown_keys` |
| 19 | `FeeDetail → Trade` 保留费用明细 | `test_to_trade_preserves_fee_breakdown` |

### 4.2 `position.py` — `PositionBook`（27 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | 必须先 `before_trading()` | `test_requires_before_trading_first` |
| 2 | **T+1**：当日买入当日不可卖 | `test_same_day_buy_then_sell_is_rejected` |
| 3 | 日初结转后可卖 | `test_sellable_after_rollover` |
| 4 | 超量卖出拒绝 | `test_oversell_rejected` |
| 5 | 当日买入不计入可卖量 | `test_only_yesterday_position_is_sellable` |
| 6 | **FIFO** 先平最早批次 | `test_fifo_consumes_oldest_lot_first` |
| 7 | 部分平仓后剩余批次正确 | `test_fifo_partial_close_leaves_correct_lot` |
| 8 | 全部平仓后清空 | `test_full_close_empties_book` |
| 9 | 同日加仓合并 + 加权平均成本 | `test_same_day_add_position_merges_and_averages_cost` |
| 10 | 跨日加仓生成新批次 | `test_different_day_add_creates_new_lot` |
| 11 | 冻结减少可卖量 | `test_freeze_reduces_sellable` |
| 12 | 冻结超持仓拒绝 | `test_freeze_more_than_position_rejected` |
| 13 | 解冻恢复可卖量 | `test_unfreeze_restores_sellable` |
| 14 | 日初清空冻结 | `test_before_trading_clears_frozen` |
| 15 | 送转：数量放大 + 成本摊薄 + 总成本不变 | `test_share_ratio_scales_quantity_and_dilutes_cost` |
| 16 | **送转取整误差用 `expected_quantity` 校正** | `test_share_ratio_corrects_rounding_with_expected_quantity` |
| 17 | 现金分红金额 | `test_cash_dividend_amount` |
| 18 | 负分红拒绝 | `test_negative_dividend_rejected` |
| 19 | 非法送转比例拒绝 | `test_invalid_share_ratio_rejected` |
| 20 | **累积**盈亏相对成本价 | `test_cumulative_pnl_is_relative_to_cost` |
| 21 | **当日**盈亏：昨仓相对**昨收** | `test_daily_pnl_uses_prev_close_not_cost` |
| 22 | 当日盈亏拆分今仓/昨仓 | `test_daily_pnl_splits_today_trade` |
| 23 | 空仓盈亏为 0 | `test_daily_pnl_zero_when_flat` |
| 24 | 买入数量校验 | `test_buy_rejects_non_positive_quantity`（参数化 ×2） |
| 25 | 卖出数量校验 | `test_sell_rejects_non_positive_quantity` |
| 26 | 批次数量非负 | `test_lot_rejects_negative_quantity` |

### 4.3 `tradability.py` — `LimitRuleTable` + `TradabilityGate`（37 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | 主板 10% | `test_main_board_ratio_10pct` |
| 2 | 主板 ST 5% | `test_main_board_st_ratio_5pct` |
| 3 | 创业板 20%（2020-08-24 后） | `test_gem_ratio_20pct_after_reform` |
| 4 | 创业板 10%（改革前） | `test_gem_ratio_10pct_before_reform` |
| 5 | 创业板 ST 仍 20% | `test_gem_st_still_20pct_after_reform` |
| 6 | 科创板 20% | `test_star_ratio_20pct` |
| 7 | 北交所 30% | `test_bse_ratio_30pct` |
| 8 | 新股窗口不设涨跌幅（2023-02-17 后主板） | `test_new_listing_window_no_limit_after_2023` |
| 9 | 2023 前不适用新股窗口 | `test_new_listing_window_not_applied_before_2023` |
| 10 | 新股窗口 5 日后失效 | `test_new_listing_window_expires_after_5_days` |
| 11 | 涨跌停价对齐 tick | `test_limit_prices_round_to_tick` |
| 12 | 涨跌停价 HALF_UP（10.03→11.03） | `test_limit_prices_round_half_up` |
| 13 | 不设涨跌幅时返回 None | `test_limit_prices_none_when_no_limit` |
| 14 | **涨停不可买** | `test_buy_rejected_at_limit_up` |
| 15 | **涨停可卖**（非对称） | `test_sell_allowed_at_limit_up` |
| 16 | **跌停不可卖** | `test_sell_rejected_at_limit_down` |
| 17 | **跌停可买**（非对称） | `test_buy_allowed_at_limit_down` |
| 18 | Bar 自带涨跌停价优先 | `test_bar_supplied_limit_prices_take_precedence` |
| 19 | 一字板（非涨跌停）双向拒绝 | `test_one_word_board_not_at_limit_rejects_both_sides` |
| 20 | 一字板涨停可卖 | `test_one_word_board_at_limit_up_allows_sell` |
| 21 | 一字板跌停可买 | `test_one_word_board_at_limit_down_allows_buy` |
| 22 | 停牌拒绝 | `test_suspended_rejected` |
| 23 | **停牌判定优先于涨跌停** | `test_suspension_takes_priority_over_limit_up` |
| 24 | 买入必须 lot 整数倍 | `test_buy_rejects_non_lot_multiple` |
| 25 | 卖出允许零股 | `test_sell_allows_odd_lot` |
| 26 | 零数量拒绝 | `test_zero_quantity_rejected` |
| 27 | 流动性裁剪 | `test_liquidity_clips_order` |
| 28 | 裁剪后对齐 lot | `test_liquidity_clip_aligns_to_lot` |
| 29 | 裁剪后为 0 → 拒单 | `test_liquidity_rejects_when_clip_rounds_to_zero` |
| 30 | volume=0 跳过裁剪 | `test_no_clip_when_volume_is_zero` |
| 31 | 可关闭流动性约束 | `test_volume_limit_disabled` |
| 32 | 使用 Instrument 的 tick_size | `test_gate_uses_instrument_tick_size` |
| 33 | 负 volume_limit_pct 拒绝 | `test_negative_volume_limit_rejected` |
| 34 | `Verdict` 辅助方法 | `test_verdict_helpers` |
| 35 | 四板块均可解析 | `test_all_boards_resolvable` |
| 36 | 未知板块报错 | `test_unknown_board_raises` |
| 37 | `Board` 枚举完整性 | `test_board_enum_membership` |

### 4.4 `account.py` — `Account`（19 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | 初始资金默认赋值 | `test_initial_cash_defaults_to_cash` |
| 2 | 负初始资金拒绝 | `test_negative_initial_cash_rejected` |
| 3 | 可用资金 = 现金 − 冻结 | `test_available_cash_excludes_frozen` |
| 4 | 冻结/解冻 | `test_freeze_then_unfreeze` |
| 5 | 冻结超额拒绝 | `test_freeze_more_than_available_rejected` |
| 6 | 解冻下限为 0 | `test_unfreeze_is_floored_at_zero` |
| 7 | 买入减少现金 | `test_buy_trade_reduces_cash` |
| 8 | 卖出增加现金 | `test_sell_trade_increases_cash` |
| 9 | 现金不足拒绝 | `test_insufficient_cash_rejected` |
| 10 | 快照 `total = cash + market_value` | `test_settle_snapshot_total_equals_cash_plus_market_value` |
| 11 | `cumulative_pnl` 相对成本 | `test_settle_cumulative_pnl_relative_to_cost` |
| 12 | **`daily_pnl` 昨仓部分用昨收** | `test_settle_daily_pnl_uses_prev_close_for_old_position` |
| 13 | 无 prev_close 时降级为累积口径 | `test_settle_without_prev_close_falls_back_to_cumulative_split` |
| 14 | 快照按日累积 | `test_settle_appends_snapshots` |
| 15 | 空仓结算 | `test_flat_account_settles_to_cash` |
| 16 | 最大可买数量对齐 lot | `test_max_buy_quantity_aligns_to_lot` |
| 17 | 价格过高时可买 0 | `test_max_buy_quantity_zero_when_price_too_high` |
| 18 | 最大可买受冻结约束 | `test_max_buy_quantity_respects_frozen` |
| 19 | 成交后现金预估 | `test_expected_cash_after_buy_and_sell` |

### 4.5 `portfolio.py` — `Portfolio`（13 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | `book()` 自动创建 | `test_book_autocreates` |
| 2 | `symbols()` 追踪 | `test_symbols_tracks_created_books` |
| 3 | 日初结转全部标的 | `test_before_trading_rolls_all_books` |
| 4 | 单标的市值 | `test_market_value_single_symbol` |
| 5 | **多标的市值** | `test_market_value_multi_symbol` |
| 6 | 缺价格报错（不静默按 0 估值） | `test_market_value_missing_price_raises` |
| 7 | 空仓不要求价格 | `test_flat_book_does_not_require_price` |
| 8 | `held_symbols()` | `test_held_symbols` |
| 9 | 总成本 | `test_total_cost` |
| 10 | 盈亏跨标的汇总 | `test_unrealized_pnl_aggregates` |
| 11 | 当日盈亏缺昨收报错 | `test_daily_pnl_breakdown_requires_prev_close` |
| 12 | 当日盈亏用昨收 | `test_daily_pnl_breakdown_uses_prev_close` |
| 13 | `has_position()` | `test_has_position` |

### 4.6 `corporate_action.py` — `CorporateActionHandler`（9 个用例）

| # | 规则 / 功能 | 对应测试用例 |
|---|---|---|
| 1 | 送转应用 | `test_share_ratio_applied` |
| 2 | 现金分红返回 | `test_cash_dividend_returned` |
| 3 | 送转 + 分红组合（先送转后分红） | `test_combined_share_and_cash` |
| 4 | 空公司行为 | `test_noop_action` |
| 5 | **配股拒绝**（阶段一不支持） | `test_rights_issue_rejected` |
| 6 | 显式开启后允许 | `test_rights_issue_allowed_when_enabled` |
| 7 | 负送转比例在构造期拒绝 | `test_negative_share_ratio_rejected_at_construction` |
| 8 | 负分红在构造期拒绝 | `test_negative_cash_per_share_rejected_at_construction` |
| 9 | 缩股（比例 < 0 且 ≥ −1） | `test_capital_reduction_ratio` |

---

## 五、读 rqalpha 源码后的修正记录

按评审要求补做了"读源码"这一步（拉取 `rqalpha/portfolio/position.py` 537 行、`account.py` 593 行原文对照），发现**实现里的两个真实缺陷**，已修：

| # | rqalpha 原文 | 修正前 | 修正后 |
|---|---|---|---|
| 1 | `position_pnl = _logical_old_quantity * (last_price - **prev_close**)` | 昨仓 `(price − cost_price)` | 拆成两个口径：`unrealized_pnl()`（累积，相对成本）+ `daily_pnl_breakdown()`（当日，昨仓相对**昨收**） |
| 2 | `handle_split` 用 `expected_quantity` 校正 round 差额 | 各批次独立 `round()`，误差累积 | `apply_share_ratio(ratio, expected_quantity=None)` 补差额到末批 |

**核验后确认无需改动的三处**：

| # | rqalpha | 本项目 | 结论 |
|---|---|---|---|
| 3 | `today_closable = _quantity − _old_quantity − 挂平今单`（可平**今**仓，期货概念） | `sellable = total − today_bought − frozen`（可卖**昨**仓） | 分析报告原文把 `today_closable` 译成"当日可卖量"是**误译**；A股语义应取"昨仓可卖"，本项目实现正确 |
| 4 | `handle_trade` 有"队列耗尽则反手开仓"分支 | 超量卖出抛 `T1Violation` | A股 T+1 下必须拒绝，本项目更严格且正确 |
| 5 | `prev_close` 是**前复权**昨收（用于算收益） | 涨跌停用**不复权** `prev_close` | 不矛盾——收益用复权口径、涨跌停用不复权口径，印证"三类价格分离"的必要性 |

**额外发现的偏差（已登记）**：`rqalpha.Account` 有 `_dividend_tax`（红利税），本项目按税前计入并显式标注，属**已知简化**。

---

## 六、与设计文档的偏差

| # | 设计文档 | 实际实现 | 理由 |
|---|---|---|---|
| 1 | 共享契约放 `data/objects.py` | 放 `do_modle/objects.py` | 让 `rules` 对 `data` 的依赖降为**零**，比"只允许 import `data/objects.py`"更强 |
| 2 | `Instrument.is_st` + `limit_ratio` 属性 | ST 由调用方按日传入 `LimitRuleTable` | ST 是逐日变化的 **PIT 状态**，不能放在静态元数据上 |
| 3 | 一字板 → 双向拒绝 | **非对称**：一字板涨停可卖、一字板跌停可买 | 原规则错误。涨停一字板买盘排队，卖单实际秒成；只有"一字板且不在涨跌停价位"才是双向枯竭 |
| 4 | `CostConfig.min_commission = 5.0` | **`0.0`（免五）** | 用户实际费率 |
| 5 | `MIN_COMMISSION_WARNING` 风控规则 | **不需要**（免五后无成本悬崖） | 前提变了，约束随之消失 |
| 6 | `CorporateAction` 无配股字段 | 增加 `rights_ratio` 用于检测 | 配股必须显式报错而非静默忽略 |

---

## 七、如何运行

```bash
PY="C:/Users/Administrator/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"

# 全量（任意 cwd 均可）
"$PY" -m pytest "D:/Codex/Some_project/Do_Modle_py"

# 单个文件（独立运行）
"$PY" -m pytest tests/test_rules_position.py -v

# 单个用例
"$PY" -m pytest tests/test_rules_tradability.py::test_sell_allowed_at_limit_up -v
```

**环境**：Python 3.13.14，隔离 venv `~/.workbuddy-ai/binaries/python/envs/default`，已装 `pytest` / `pyyaml`。
`rules/` 层不需要 pandas / numpy —— 这也是"零外部依赖"约束的价值：规则层可在任何环境单测。

---

## 八、P1 验收门槛核对

| 门槛（设计文档 §7） | 状态 |
|---|---|
| T+1 超卖被拒 | ✅ 用例 2/3/4/5 |
| 涨停不可买 / 跌停不可卖 | ✅ 用例 14/16 |
| 停牌、一字板被拒 | ✅ 用例 19/20/21/22/23 |
| 往返成本率 ≈ 0.152%（不含滑点）/ 0.252%（含滑点） | ✅ 用例 11/12 |
| 除权后数量与成本正确 | ✅ 用例 15/16 |
| 4 条 import 禁令自动断言通过 | ✅ `test_architecture.py` 7 个用例，且已验证会真实失败 |

**P1 门槛全部达成，可进入 P2（`data/` 数据层 + `PITGuard`）。**
